# 復旧計画（1枚） - 一人麻雀 / mugen-mahjong-red

前提：本アプリはサーバーDBを持たない。守るものは「リポジトリの中身
（コード・素材・ドキュメント）」のみ。→ [[バックアップ手順]] 参照。

| # | シナリオ | 戻す元 | 手順 | 想定復旧時間 |
| --- | --- | --- | --- | --- |
| 1 | ローカルPCが壊れた／紛失した | GitHub（origin） | 新しいPCで`git clone`＋動作確認（下記参照） | 15〜20分 |
| 2 | 誤って作業フォルダを削除した（pushは済んでいる） | GitHub（origin） | 上と同じく`git clone`し直す＋動作確認 | 15〜20分 |
| 3 | バグを含んだ変更をpushしてしまい、`main`が壊れた | 直前の正常なコミット | `git log`で壊れる前のコミットを特定し<br>`git revert <該当コミット>`<br>→ 動作確認後 `git push` | 10〜15分 |
| 4 | GitHubアカウント自体のリスク（乗っ取り／private repo誤削除／2段階認証デバイス紛失） | ①ローカルの最新クローン、無ければ②週次zipバックアップ | 詳細は下記「GitHubアカウント自体のリスク」参照 | 30分〜1時間程度＋動作確認 |

## git clone後の動作確認（1・2の所要時間に含む）

`git clone`が終わった時点では「ファイルが揃った」だけで、「遊べる状態」では
まだない。以下を必ず確認してから復旧完了とする。

- [ ] `index.html`をブラウザで開き、`images/tiles/`の牌画像が正しく表示されるか
      （パス切れで牌が表示されない事故が起きやすい）
- [ ] `sounds/`の効果音・ボイスが鳴るか（1半荘分軽くプレイして確認）
- [ ] **VOICEVOX関連の設定をやり直す必要はない** — 配布版のズンダモン音声は
      開発環境のVOICEVOXで事前生成した静的ファイル（`sounds/voice-*.ogg`）を
      同梱しているだけで、配布版自体は実行時に127.0.0.1（VOICEVOXエンジン）へ
      通信しない。したがって新しいPCでも**このアプリを動かすだけなら
      VOICEVOXのインストールもCORS Policy Mode設定も不要**。
      ※ただし「新しいボイスを追加する」など**開発を続ける**場合は、
      新しいPCにVOICEVOX Engineを別途インストールし、CORS Policy Modeを
      許可する設定をやり直す必要がある（これはgit管理外のローカル環境設定のため、
      バックアップからは復元されない）。

## GitHubアカウント自体のリスク

「GitHubに置いてあるから安心」と思いがちだが、以下はどれも
**アカウント単位の事故**であり、リポジトリの中身が無事でも
アクセスできなくなる可能性がある。

| 想定シナリオ | 内容 |
| --- | --- |
| アカウント乗っ取り | パスワード漏洩等で第三者にアカウントを乗っ取られ、リポジトリを操作・削除される |
| private リポジトリの誤削除 | 自分の操作ミスで`mugen-mahjong-red`（private）を削除してしまう |
| 2段階認証デバイスの紛失 | 2段階認証の端末（スマホ等）を紛失し、自分自身がログインできなくなる |

### アクション項目（今すぐ確認する）

- [ ] **2段階認証のリカバリーコードを、GitHub以外の場所に保管してあるか、今すぐ確認する**
      （保管先の例：パスワードマネージャー、印刷して物理的に保管、など。
      GitHubアカウントの中や同じGoogle/Microsoftアカウント直下に置くのは避ける）

## これが無かったら怖い点

- シナリオ4（GitHubアカウント自体のリスク）が一番怖い。GitHubは
  「別の場所」に見えて、**同じアカウントに紐づく限り単一障害点**になり得る。
  週次zipという「アカウントの外」のコピーと、2段階認証のリカバリーコードの
  保管場所——この2つがあるかどうかで、最悪ケースの復旧可否が決まる。
- シナリオ1・2は「git cloneすれば終わり」ではなく、素材の読み込み確認まで
  やって初めて復旧完了。ここを飛ばすと「pushはできているのに、いざ動かしたら
  牌画像が出ない」といった事故に気づくのが遅れる。
- シナリオ3は「戻し方」を知らないと、慌てて`reset --hard`で
  さらに履歴を壊すリスクがある。事前に`revert`を使う方針を
  決めておくことが重要。

## 復旧の判断フロー

```
問題発生
  ├─ ローカルが無事？ → はい → git push し直すだけで解決することも
  │                    → いいえ → GitHubのクローンで復元（シナリオ1,2）
  └─ GitHub側が壊れた／消えた？
       ├─ ローカルに最新版が残っている → 新リポジトリへpushし直す
       └─ ローカルも失っている → 週次zipバックアップから復元
```
