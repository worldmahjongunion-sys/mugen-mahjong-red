# バックアップ手順（一人麻雀 / mugen-mahjong-red）

## 1. このアプリで守るべきデータ

サーバー側データベースは無い（ブラウザ完結のゲームのため）。対局中の状態は
`sessionStorage`のみで、タブを閉じれば消える一時データなのでバックアップ対象外。

守るべきものは次の3つ（＝リポジトリの中身そのもの）。

| 対象 | 中身 |
| --- | --- |
| ゲーム本体コード | `index.html` |
| 素材 | `images/`（牌画像）, `sounds/`（効果音・ボイス） |
| ドキュメント | `麻雀アプリ_基準.md`（仕様の蓄積）, `バグ報告_和了.md`, `CPU_AI改善案_終盤打点判断ロジック.md` |

## 2. 今すでにできていること

- 全ファイルが git 管理下にある（未追跡ファイルなし、確認済み）
- `git push` により GitHub（`origin`
  = https://github.com/worldmahjongunion-sys/mugen-mahjong-red）に
  コピーが作られる → これが「別の場所への保存」に相当する

## 3. 抜け穴（これが無いと怖い点）

1. **commitしたがpushし忘れた変更が、複数日ぶん溜まりやすい** →
   このプロジェクトは`git add -A`/`git add .`を使わず、変更を項目ごとに
   分割してcommitする運用にしている（過去には`hash-object`/`update-index`で
   作業ツリーを段階的に組み立てたこともある）。通常の「まとめてadd→commit」
   より一手間かかる分、区切りをつけるタイミングが遅れがちで、
   「数日分の変更がローカルにしかcommitされていない」状態が起きやすい。
   その状態でPCが壊れる／ディスクが飛ぶと、複数日分の作業が丸ごと消える。
2. **GitHubアカウントの乗っ取り・誤操作でのリポジトリ削除** → 唯一の避難先が
   同じアカウント配下にある限り、アカウント単位の事故には無力。
   具体的なシナリオと対策は [[復旧計画]] の「GitHubアカウント自体のリスク」参照。
3. **ローカルの作業フォルダ自体を誤って削除**した直後、pushもしていない
   → 直前の作業がまるごと消える。

## 4. バックアップ手順（毎回の作業終わりに）

```bash
# 1. 変更を確認
git status

# 2. 区切りの良いところでcommit
git add -A
git commit -m "変更内容の要約"

# 3. GitHubへ反映（＝バックアップ）
git push
```

作業を終えるときは、**必ず3までやる**（commitだけで満足しない）。

## 5. 週1回の二重バックアップ（GitHubアカウント自体の事故に備える）

GitHubだけに頼らないよう、週1回、フォルダ全体をzipにして
GitHubとは別の場所（OneDrive／Googleドライブ／外付けHDDなど）に置く。

```powershell
# PowerShellで実行（日付入りzipを作る）
$date = Get-Date -Format "yyyy-MM-dd"
Compress-Archive -Path "C:\Users\piedr\Documents\mugen-mahjong-red" `
  -DestinationPath "C:\Users\piedr\OneDrive\backup\mugen-mahjong-red_$date.zip"
```

置き場所は「GitHubを使っているのと同じGoogle/Microsoftアカウント」以外が理想だが、
まずは「別のクラウド or 物理ドライブに置く」ことを優先する。

## 6. 対象外にしたもの

`一人麻雀_配布用.zip`（`C:\Users\piedr\OneDrive\Desktop\`）などの配布用zipは、
中身がリポジトリ内の`index.html`・`images/`・`sounds/`をそのまま固めた
再生成可能なコピーに過ぎないため、今回の「守るべきデータ」には含めない
（リポジトリさえ守れれば、配布用zipは作り直せる）。
