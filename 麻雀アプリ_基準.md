# 麻雀アプリ — 基準.md

最終更新: 2026-08-31（局の継続・親交代を実装）
状態: 稼働中
保存場所: GitHub（~/mugen-mahjong-red にクローン済み）
リポジトリ: https://github.com/worldmahjongunion-sys/mugen-mahjong-red （Private）

---

## 概要

一人麻雀(あなた＋CPU3人)のWebアプリ。単一の `index.html` で完結する構成。
自作SVGでの牌デザイン、非常に多くのカスタム役・独自点数計算ルール（符計算なし、役なし和了、役満固定点数など）を実装済み。

作業は Claude Code でこのフォルダ（~/mugen-mahjong-red）を直接編集する形で行っている。

---

## 重要ファイル

| ファイル | 役割 |
|---|---|
| `index.html` | アプリ本体（HTML/CSS/JS一式） |
| `基準.md` | このプロジェクトの正本（このファイル） |

---

## 現フェーズ

STEP1〜5（見た目・配牌・進行・鳴き・和了判定/役/点数計算）完成。
その後もSTEP5の範囲でリーチ・牌画像素材への差し替え・レイアウト実測ベース化・
フリテン・リーチ後の暗槓制限・一発などを継続中。
STEP6（CPU AI強化）・STEP7（仕上げ）は未着手。局の継続と親交代は2026-08-31に実装済み
（詳細は下記「半荘進行（局・本場・親交代・終了条件）」参照）。

---

## 次のアクション

- [ ] 下記「2026-08-11 第2弾」の残項目（9・10）の確認・対応
- [ ] 実機でのリーチ関連の動作確認（宣言・供託・自動ツモ切り・鳴き禁止が意図通りか）
- [ ] 未実装の役：海底摸月・河底撈魚・嶺上開花・槍槓（槍槓はCPUの加槓自体が未実装なのが前提）・
      ダブルリーチ（巡目を数える仕組み自体が無い）・九蓮宝燈
- [ ] 局の継続と親交代（現在は東1局のみで終了する構成）
- [ ] CPU思考の強化（STEP6、現状は簡易ランダム／単純条件のAIのみ）
- [ ] リーチ後の暗槓制限（`canAnkanAfterRiichi`）は`checkAnkanOpportunity`（自分専用の暗槓チェック）
      にしか入っていないため、**CPUの暗槓（`executeCpuAnkan`を呼ぶ側）には効いていない**。
      CPUはそもそもリーチしない仕様のため実害は無いはずだが、CPUリーチを将来実装する場合は要対応

---

## 対応中の修正リスト（2026-08-08〜）【完了】

卓レイアウト作り直し（3重ring構造・牌サイズ統一・山2段積み表示・鳴き選択の1段階化）の
続きとして、実際に触って気づいた不具合を修正した第1弾。

- [x] 1〜5（旧リスト）は下記2026-08-09のリストに詳細化・再掲して対応中
- [x] 6. 加槓の表示を実卓と同じに：ポンした横向き牌の上に4枚目を重ねて横向きで置く
- [x] 7. 中央BOXの中身を減らし左上に移す：「東n局n本場」「供託n本」「表ドラ・リーチドラ」を左上へ（結果表示＝右上と対角）。中央BOXには各自の点数欄と残りの山の数だけ残す
- [x] 8. 下家の方角マーク「南」の位置と向き：一度直したが実機確認で再度ズレを指摘されたため、下記2026-08-09のリスト6で方式ごと作り直す
- [x] 9. 上家の方角マーク「北」も同様に確認：同上、6で作り直す

---

## 対応中の修正リスト（2026-08-09〜）【実装済み・実機確認待ち】

実機で触った結果のフィードバックを反映。すべてコード上は対応済みだが、私（Claude Code）は
画面を実際にレンダリングして確認できないため、実機での見た目確認はユーザー側で行う必要がある。

- [x] 0. バグ：左上の表示が「ドラ／裏ドラ」になっている→「ドラ／リーチドラ」に修正
- [x] 1. 牌のサイズ統一＋最大化：`--back-*`を廃止し全牌`--tile-h`/`--tile-w`に統一。JS(`fitTileSize()`)が
      `.ring.hand-ring`の実サイズを実測しながら二分探索し、画面の縦横両方に収まる最大サイズを自動計算する
      方式に変更（手計算の式ではなく実測ベース）。resize時にも再計算する
- [x] 2. 中央BOXの初期揺れ：手牌エリアは既存のpadCount機構で常に14枚分の幅を確保済みだったため、
      鳴き面子エリア(`.meld-row`)に最大4組ぶんのmin-width/heightを追加。点数表示もmin-width:5ch+
      text-align:rightで桁変化による揺れを防止。ネームプレート4辺のセルはJS(`stabilizeCenterInfoCells()`)
      で実測固定
- [x] 3. 左上の文字サイズを右上の和了結果表示(13px)と揃えた
- [x] 4. 鳴いた牌と手牌の間隔ゼロ化：手牌と鳴き面子を1つのinline-flexコンテナにまとめgap:0に統一。
      `repositionMeldRow`とその関連関数（`slotSize`/`tileVar`）は不要になったため削除
- [x] 5. 鳴き操作の1段階化：`callComboOptions()`で組み合わせ数を判定し、1通りならボタン押下で即成立、
      2通り以上（赤5が絡むポン・チーなど）のときだけ従来通り手牌クリックで選ばせる。カンは常に1通りなので
      必ず即成立する
- [x] 6. 上家・下家の自風マークの向き：writing-mode＋打ち消し回転をやめ、プレート全体に
      transformを1回だけかける方式に作り直した。上家=rotate(90deg)、下家=rotate(-90deg)、
      対面=rotate(180deg)。下家のHTML順も「方角→点数」に統一。箱の実寸問題はJSで実測固定して回避
- [x] 7. 下段ボタンの幅統一：`grid-template-columns: repeat(11, 1fr)`にした

---

## 対応中の修正リスト（2026-08-09 実機フィードバック第2弾）【実装済み・実機確認待ち】

第1弾の実機確認で見つかった、主に「対称性」に関する不具合の修正。

- [x] 1. 手牌の位置崩れ＋鳴き牌との隙間（同一原因）：`.meld-row`に予約した最大4組ぶんの幅が
      非対称（手牌の片側だけ）だったのが原因。予約を`--meld-reserve`(牌2枚ぶんの最低値)に縮小し、
      手牌の反対側に同じ幅の`.hand-spacer`を追加。鳴きが実際にそれより大きい時は`syncHandSpacer()`が
      鳴き枠を実測して空きスペーサーを同じ幅に揃えるため、鳴きが何組でも手牌の中心は動かない
- [x] 2. 捨て牌エリアの寸法確保：min-width/min-heightをwidth/heightに変更し、6×3（縦持ちは3×6）の
      実寸を直接固定する形に書き直した。sedによる変数名の破損は無かった（grep再確認済み）が、
      min-widthよりwidth固定の方が確実なため変更。7打目・13打目のジャンプが再発しないか実機確認が必要
- [x] 3. 卓の基準幅：自分の山を王牌と地山の間に空き2枚を挟んで11枚分に統一（他家と同数に）。
      `fitTileSize()`は実測ベースのままとし（手計算の式より確実なため維持）、実際に描画された卓の幅を
      `--table-width`としてJSで反映する方式にした
- [x] 4. 下段ボタンを卓幅に収める：`.button-row`に`max-width:var(--table-width)`と`margin:0 auto`を追加
- [x] 5. 「残り牌」表示：ラベルと数字を別要素にして縦積みにし、数字だけfont-size:16pxに拡大。
      `min-width:2ch`で桁数変化による揺れを防止
- [x] 6. 暗槓の候補数判定：`findAnkanTile`が最初の1種類しか返さない問題を修正し、
      `findAllAnkanTiles`で全候補を返すように変更。2種類以上同時に4枚揃った時は
      候補ごとにボタンを出して選ばせる（`callComboOptions()`とは別軸の判断のため、
      同じ関数には乗せず`findAllAnkanTiles`という並行した仕組みにした）
- [x] 7. 削除した関数の残存呼び出し：`repositionMeldRow`/`slotSize`/`tileVar`の呼び出しは
      grep確認の結果、残っていなかった（削除時に呼び出し側も一緒に消していた）

※ 私（Claude Code）はブラウザで実際にレンダリングして確認する手段を持たないため、
上記はすべてコード上の対応であり実機確認が必須。特に項目2（捨て牌エリア）は
診断結果次第で追加対応が必要になる可能性がある。

---

## 対応中の修正リスト（2026-08-09 実機フィードバック第3弾）【実装済み・実機確認待ち】

- [x] A. 捨て牌が中央BOXと重なる（最優先）：真因は`--tile-w`/`--tile-h`の計算ミスではなく、
      左右の捨て牌（90度回転）で牌のレイアウト箱が回転前のまま（横長のセルに縦長の箱を置く形）に
      なっていたこと。`transform:rotate()`をCSSで牌全体にかける方式をやめ、鳴き牌と同じ
      `applyMeldRotation()`（箱自体を回転後の実寸にし、中のSVGだけ回転させる）を`renderDiscard()`に
      適用して解消した
- [x] B・C. 鳴き面子と手牌の距離：DOM挿入順は確認したが不正はなかった（スペーサーは常に手牌を
      はさんで鳴き枠と反対側）。ご指摘通り間隔を0→牌の幅1枚分(`gap:var(--tile-w)`)に変更した
- [x] D. 鳴きで中央BOXが揺れる：`syncHandSpacer()`をDOM実測(`offsetWidth`)からmelds配列の
      組数による直接計算(`meldReserveCss()`)に変更し、鳴き枠と反対側スペーサーを同一フレームで
      同じ値に設定するようにした
- [x] E. 狭い画面での重なり：右上・左上の情報パネルのfont-size/paddingを`--tile-h`に連動する
      `clamp()`/`em`指定に変更し、卓が縮むと一緒に縮小するようにした

※ Bについては明確な原因コードを特定できなかった。C・Dの変更で解消していることを期待するが、
まだ隙間が残る場合は改めて報告してほしい。

追加指示：中央BOXを牌7枚分の正方形にする件も対応済み。
- [x] 中央BOX・捨て牌エリアの実測ベース化：捨て牌1枚をdiscardBottomグリッドに実際に置いて
      `getBoundingClientRect()`で実測し（`measureDiscardTile()`）、そこから捨て牌エリア4方向と
      中央BOX（捨て牌エリアの実測幅6枚分＋余白1枚分＝7枚分の正方形）のサイズを算出する
      `applyMeasuredLayoutSizes()`に置き換えた。`--tile-w`をそのまま6倍する計算はやめ、
      旧`squareCenterBox()`は削除

---

## 和了バグ修正（2026-08-09）詳細は `バグ報告_和了.md` 参照

- [x] バグ①：鳴き（チー）が絡む手で役の取りこぼし（タンヤオ＋三色同順が役なし判定になる）
      原因：`meldsToSets()`がチーの牌を`[手牌2枚, 鳴いた牌]`の順でそのまま並べていたため、
      鳴いた牌が順子の一番小さい数字だった場合に`tiles[0]`が順子の起点にならなかった。
      三色同順をはじめ`s.tiles[0].num`を順子の起点として使う役判定（十数箇所）が軒並み影響を受ける。
      `meldsToSets()`でsequence（チー）だけ数字順にソートするよう1箇所で修正し、実際にNode.jsで
      再現テスト（チーで2mを呼んで3m4mを晒す234m＋234p＋234s＋678p＋55sの手）を行い、
      修正前は三色同順が抜け落ち、修正後は正しく1翻で乗ることを確認した
- [x] バグ②：役なしツモの精算が3倍になっていた
      原因：`settleYakunashi()`のツモ計算で、基礎点+ドラ分（合計額のはず）を3人で割らずに
      そのまま各人へ請求していた。赤牌ぶん（1人あたりの額）は元々正しかった。
      基礎点+ドラ分だけをMath.ceilで100点単位に切り上げて3等分するよう修正し、
      Node.jsでのテストで「親・ドラ1・赤2枚・ツモ」が9000点（3000点オール）になることを確認した
      （修正前は27000点＝3倍になっていた）

---

## 対応中の修正リスト（2026-08-11〜）

前回セッションで結果表示の全面書き換え（1行1役形式・役なしツモ精算の子1親2比率化）と
中央BOXネームプレートの中央揃えCSSまで対応済み（未コミット）。それに続けて依頼された6件。
1〜4を先に対応し、5・6は分けて対応してよいとの指示。

- [x] 1. 保留にしていた`.result-row`のCSSを適用：役名/飜数/点数の3列グリッド、
      点数列は右寄せ+tabular-nums、合計行は上に罫線+やや大きめ
- [x] 2. 王牌のリーチドラ表示牌にも、表ドラ表示牌と同じ赤い枠を付ける
- [x] 3. 流局時の表示から「テンパイ／ノーテン」の文言を削除し、点数だけにする
- [x] 4. 役名「門前清自摸和」の表示を「門前清」に短縮（表示名だけ、役判定キーは変更しない）
- [x] 5. 暗槓の表示を実機仕様に修正：積む1枚を席によらず常に表向きにし（旧`isSelf`分岐を撤去）、
      赤牌があれば積む1枚に優先して選ぶよう`renderMelds`のankan分岐を書き換えた。
      未使用になった`STACKED_BACK_CLASS`定数も削除
- [x] 6. カンで積んだ牌が王牌と重なる不具合：`.meld-row.has-kan`（CSS）を追加し、
      `renderMelds`が席のmeldsに暗槓・大明槓・加槓のいずれかがあれば付与、無ければ外す形にした。
      厚みは牌の横幅ぶん(`var(--tile-w)`)加算。カンの無い席は`.has-kan`が付かないため
      既存の`var(--tile-h)`のまま変化しない（上家の暗槓による手牌との重なりも同一原因のため解消）

---

## 対応中の修正リスト（2026-08-11 第2弾）

上記6のカン重なり対応は保留のまま、新たに依頼された4件。

- [x] 7. 和了結果パネルの折り返し・はみ出し修正：コミット`cfd4241`（2026-08-11）で対応済み
- [x] 8. 和了結果の「あなた」表記を「自家」に変更：`seatLabel`（`bottom:"自家"`）で対応済みと
      コード上確認（index.html内 seatLabel定義）
- [~] 9. チーの操作省略：`callComboOptions()`による「組み合わせが1通りなら即成立」の
      既存の仕組み（2026-08-09導入）がチーにも適用されており、実質的に満たされている可能性が高い。
      ただし「1枚目クリック後、2枚目の候補が1通りに絞られた時点」での即確定は未実装
      （`onSelectCallTile`は必要枚数に達するまで確定しない）。要件の解釈次第で追加対応が必要
- [ ] 10. 調査のみ・未対応：自分(bottom)の捨て牌をCPUがポンした時、
      どこから鳴いたかを示す横向き牌の位置が正しいか。`meldCalledPosition(name, calledFrom)`が
      `calledFrom === "bottom"`で何を返すか確認。大明槓が山と重なって見えなくなっていないかも確認

---

## 対応済み（2026-08-15〜08-21・ドキュメント未反映分をまとめて追記）

前回のドキュメント更新（08-11）以降、実機フィードバックを受けて以下を対応済み。
コミットログのみに残っていた内容のため、ここにまとめて反映する。

- [x] 鳴き牌グループの厚みを卓の外側の辺へ揃える（`c318c3d`, 08-15）：
      `group.style.alignItems`が`center`だと積み牌の増えた厚みが内外に半分ずつ配分され、
      山と重なっていた。外側の辺(top/bottom/left/right)基準に変更。
      あわせて鳴き・カン系4関数の末尾で`fitTileSize()`を呼び直し、カンで鳴き行が厚くなった後も
      手牌サイズが再計算されるようにした
- [x] 相手の伏せ手牌を上から見た薄い帯表示に変更（`50d5321`, 08-16）：
      厚み=牌の高さの半分(`--tile-thick`)、象牙色75%/青25%グラデーションで牌を横から見た
      二色を表現。青（裏側）が卓中央を向くよう席ごとに向きを分ける。暗槓の伏せ牌は既存の
      `.tile-back`（柄付き）のまま維持し、`cpuHandBackClassByName`で区別。捨て牌の枠線も細くした
- [x] `fitTileSize()`の二分探索中は卓を不可視にする（`58eaee9`, 08-19）：
      サイズ確定までのちらつきを防止
- [x] 牌の表を麻雀豆腐の画像素材（`images/tiles/*.png`）に差し替え（`4b87d8e`, 08-19）：
      自作SVGから画像素材へ移行。`applyMeldRotation`を画像(`<img>`)対応に変更
- [x] リーチを実装（`5b833c9`, 08-20）：宣言・供託・自動ツモ切り・鳴き禁止・リーチ1翻。
      結果パネルにリーチ供託の行を追加
- [x] カン後の嶺上牌でも暗槓を検出できるよう修正、結果パネルのリーチドラ・赤牌の名前だけを
      赤字にする表示調整（`b3f64a4`, 08-21）

---

## 対応済み（2026-08-24〜08-25）

前回のドキュメント更新（08-24、コミット`5548f1d`）以降の3件。いずれも実装前に既存コードを
調査してから着手し、Node.js（`vm`モジュールでDOMをダミー化し、`index.html`のスクリプト全体を
実際に実行するハーネス）で本物の関数を使って検証している。

### 1. フリテン管理（`cdc8036`）

- `getWaits(seat)`を新設し、`isTenpai`は`getWaits(seat).length > 0`に置き換え（判定ロジックを
  二重に持たない）
- `discardHistory[seat]`を追加。`discards[seat]`（卓上に見えている捨て牌。鳴かれると消える）とは別に、
  捨てた牌の種類キー（`tileKey`の文字列）だけを鳴かれても消さずに積み続ける
- フリテン状態は2つ：`furitenTemp`（同巡内、自分の次のツモで解除）と`furitenRiichi`
  （リーチ後の見逃しによる永続、局が終わるまで解除しない）
- `isFuriten(seat)` / `canRon(seat, tile)`を新設し、自分のロン判定・CPUのロン判定の両方を
  `canRon`経由に統一
- 見逃しの記録は3経路：スルーボタン、鳴きの組み合わせが1通りで即成立する経路、
  鳴き牌の選択を確定した経路
- CPUは「ロンできたら必ずロンする」簡易AIのため見逃しという概念が無く、`furitenTemp`は
  CPU側では実質使われない。ただし恒久フリテン（`discardHistory`による判定）はCPUにも効く
- 実機確認済み：1筒・4筒の両面待ちで4筒をツモ切りした後、上家から1筒が出ても和了ボタンが
  出ず、チーのみ選択可能だった

### 2. リーチ後の暗槓制限（`4028a87`）

ハウスルール：リーチ後の暗槓は、待ちが変わらなくても牌姿（面子の解釈）が変わるものは禁止。
完全に独立した暗刻からの暗槓のみ可とする。

- `findAllDecompositions(concealedTiles)`を新設。既存の`findDecomposition`は最初に成立した
  1通りで打ち切るため流用できず、あり得る分解を全て列挙する版を別に作った
  （既存の`findDecomposition`/`decomposeMelds`は無変更）
- `canAnkanAfterRiichi(tile)`：対象牌を1枚除いた13枚について待ちを全て求め、各待ち牌を
  加えた14枚の全分解で、その3枚が独立した暗刻（他の面子・雀頭に一切使われない）として
  現れる場合だけ`true`
- `checkAnkanOpportunity`はリーチ中だけ候補を`canAnkanAfterRiichi`でフィルタする形にし、
  非リーチ時の挙動は変更していない
- 本ルールは七対子の「同じ牌4枚を2対子として使う」を認めているため、標準形に一切分解
  できない待ちが実在する（例：5m×4＋1p1p+3p3p+5p5p+7p7p+9p単騎）。この場合は
  「独立した暗刻」を確認できないため安全側で禁止にする

### 3. 一発（`4f08336`）

- `ippatsuActive`という1つのフラグで管理。CPUはリーチしないため自分（bottom）専用
- 立てる：`onDiscardMyTile`でリーチ宣言牌を切り終えた時
- 折る①（鳴き）：`executeCall`/`executeChi`/`executeCpuAnkan`/`executeAnkan`/`executeKakan`の
  各冒頭（自分自身の暗槓でも折る。リーチ後の暗槓は2で実装済みのため実際に起こりうる）
- 折る②（一巡）：`playerDraw`の自動ツモ切りで、`onDiscardMyTile`を呼ぶ**前**に折る
  （打牌の結果を待たず、この打牌をもって先に失効させる。ツモ和了した場合はここに来ないため
  一発ツモは正しく成立する）
- 役の加算はリーチと同じ2箇所（`computeYaku`と七対子の手動加算）に、
  `riichiDeclared[seat] && seat === "bottom" && ippatsuActive`の条件で追加。
  国士無双はリーチ自体を加算していないため今回も対象外
- 検証：リーチ宣言→他家の打牌でロン／リーチ宣言→自分の第一ツモでツモ和了／
  リーチ宣言→他家がポン→その後和了、の3ケースで一発の有無が正しいことを確認

---

## 半荘進行（局・本場・親交代・終了条件）（2026-08-31）

それまで東1局で必ず終了していた実装を、半荘（東場を2周＝東1〜8局、UI上は連番表示）を
最後まで進行できるように変更した。

### ルール仕様

- 開始持ち点：4人とも35,000点
- 「上がり連荘」ルール：連荘するのは親が和了（ツモ・ロン）した場合のみ
  - 親が和了：連荘（親そのまま）・本場+1・局数据え置き
  - 流局：親のテンパイ／ノーテンを問わず常に親交代・局数+1・本場0にリセット
    （2026-08-31当初は「親テンパイなら連荘」というリーチ麻雀の一般ルールで実装したが、
    同日中に「上がり連荘」＝流局は常に親交代へ変更した。ノーテン料の精算自体は変更していない）
  - 子の和了：親交代・局数+1・本場0にリセット
- 本場ボーナス：ロンなら放銃者から+1,500点/本場、ツモなら他家3人からそれぞれ+500点/本場
  （2026-08-31当初は+300点/本場・+100点/本場で実装したが、同日中に5倍へ変更した）
- 終了条件（優先順）：①誰かがマイナス（トビ）②誰かが70,000点超え③東8局が終了
  （②③は「その局の結果が連荘扱いかどうか」に関わらず、条件を満たした時点でそのまま終了する。
  東8局中に親が連荘条件を満たしても「9局目」には進まない）
- 終了時は最終順位（持ち点順、同点はあなた→下家→対面→上家の順）を全画面オーバーレイで表示する

### 実装方式：sessionStorage + location.reload()

このアプリは「配牌〜終局」が1回きりの手続き型スクリプトとして書かれており（`wall`/`hands`/
`discards`/`melds`/フリテン各種フラグ等がすべてトップレベルの`const`/`let`で一度だけ初期化される
構成）、次局への移行を同一ページ内で完結させるには大規模なリファクタが必要だった。
そこで、局をまたぐ状態（持ち点・局数・本場・親の座席）だけを`sessionStorage`
（キー`mugenMahjongRedMatchState`）に保存し、次局は`location.reload()`でページ全体を
作り直す方式にした。配牌・進行・和了判定・役・点数計算の既存ロジックは一切変更していない。

- `loadMatchState()`/`saveMatchState()`/`clearMatchState()`：sessionStorageの読み書き（index.html冒頭、
  配牌処理の直前に定義。`DEALER_SEAT`が配牌の内容にも関わるため、この読み込みだけは他の半荘状態
  （`scores`/`kyokuNumber`/`honba`）より前、ファイルの早い位置で行っている）
- `computeNextMatchState(current, renchan)`：連荘/親交代の次状態を計算する純粋関数
- `checkMatchEnd(scores, finishedKyokuNumber)`：終了判定（トビ＞70,000点超え＞東8局終了の優先順）
- `applyHonbaBonus(honbaCount, isTsumo, winnerSeat, loserSeat)`：本場ボーナスの精算
- `proceedOrEndMatch(renchan)`：`finalizeWin`/`handleRyuukyoku`の末尾から呼ばれる共通処理。
  終了条件を満たせば`showFinalStandings()`で最終順位オーバーレイを表示、満たさなければ結果パネルに
  「次局へ」ボタンを追加し、押されたら状態を保存してリロードする
- `renderMatchHeader()`：局表示（「東n局 m本場」）・自風マーク・起家（＝その局の親）マークを
  `DEALER_SEAT`/`SEAT_WIND`/`kyokuNumber`/`honba`に合わせて描き直す

### 親交代に伴う副次的な変更（画面位置は固定、風だけが親を基準に回る）

このアプリは自分（bottom）が常に画面下・CPU3人が top/left/right 固定という設計のため、
親交代時に「回転する」のは自風表示（東南西北）と起家マークだけで、画面上の座席配置自体は
変えていない（実卓のような座席の物理回転はしない簡略化）。場風は東場2周のため常に東（`ROUND_WIND=1`）
のまま固定。

これに伴い、`DEALER_SEAT`はそれまでの`const "bottom"`固定から、局ごとに`NEXT_SEAT`で回りうる
`let`変数に変更した。あわせて、配牌直後の「最初の1枚」を常に自分に配っていた処理も、
親の座席に応じて`playerDraw()`（親が自分の場合）または`cpuDrawAndDiscard(DEALER_SEAT)`
（親がCPUの場合）を通常のツモと同じ経路で呼び出す形に変更した。CPUが親の場合は
`advanceTurnAfterDiscard`の連鎖で自動的に手番が進み、自分の番になった時点で自然に
`playerDraw()`につながる（鳴き・ロンの判定も通常のツモと同様に働く）。

### 結果パネル表示：「◯本場」の内訳行とALL/合計の合算、両方を表示（2026-08-31）

結果パネルには「◯本場 1500」のような本場ボーナスの内訳行と、本場込みの実額を合算した
「3500ALL」（ツモ）／合計行（ロン）の両方を表示する。一時的に内訳行を削除して合算のみに
した版を作ったが、同日中に「両方表示する」形へ戻した。

`finalizeWin()`内で`paymentsBySeat`（各家の実際の支払額）を計算するタイミングを
`applyHonbaBonus()`適用後にしている（供託加算とは独立。供託は勝者のscoresにしか
加算されないため他家の支払額には影響しない）ため、`paymentsBySeat`/`totalGain`は
自動的に本場ボーナス込みの実額になる。`applyHonbaBonus()`の戻り値（本場ボーナス合計）は
`honbaBonusTotal`として`showResult()`にも渡し、`honba`（本場数）とあわせて内訳行の描画に使う。
点数移動そのものの計算式（`settleTsumo`/`settleRon`/`applyHonbaBonus`等）は変更していない。

### テスト（Node.jsハーネス）

既存の「vmモジュールでDOMをダミー化し、index.htmlのスクリプト全体を実際に実行する」方式で
ハーネスを新規作成し（スクラッチパッド配下、リポジトリには未コミット）、以下を実関数呼び出しで検証した。
41件すべて成功、乱数（山のシャッフル・CPUの簡易AI）に依存する複数回の再実行でも安定して成功している。

- 純粋関数の単体テスト：`computeNextMatchState`（連荘／親交代でのNEXT_SEAT遷移）、
  `applyHonbaBonus`（ロン＝放銃者から1,500点/本場、ツモ＝他家3人から500点/本場ずつ）、
  `checkMatchEnd`（トビ／70,000点超え／東8局終了、および優先順位）
- 統合テスト（`finalizeWin`/`handleRyuukyoku`を実際に呼び出し、`sessionStorage`への保存内容や
  最終順位オーバーレイの表示を検証）：
  - 親のツモ和了→連荘
  - 親のロン和了（本場2持ち越し）→連荘・本場3・本場ボーナス3,000点が精算に反映されている・
    結果パネルに「2本場 3000」の内訳行と、本場込みの合計行（6000点）の両方が表示されている
  - 親のツモ和了・3本場（2000+本場ボーナス1,500=3,500/人）→「3本場 4500」の内訳行と、
    「3500ALL」として本場込みの実額に合算した行の両方が表示される
  - 流局・親テンパイでも親交代（上がり連荘ルールのため、テンパイでも連荘しない）
  - 流局・親ノーテン→親交代
  - 子の和了→親交代
  - 東8局終了→最終順位画面表示・sessionStorageクリア（連荘条件を満たしていても終了する）
  - トビ→即終了
  - 70,000点超え→即終了
  - 「次局へ」で保存した状態を新しいセッションで読み込み、持ち点・本場が正しく引き継がれる
  - 親がCPU（bottom以外）でも、CPUの手番が連鎖して最終的に自分の手番まで正しく進む

### 未検証（実機確認が必要）

Node.jsハーネスはDOMをダミー化した状態でのロジック検証であり、実際のブラウザでの見た目
（局表示・自風マーク・起家マークの切り替わり、「次局へ」ボタンの見た目、最終順位オーバーレイの
レイアウト）は実機で確認していない。

## 続きを頼む時の一言

「麻雀アプリの続きをやりたい」と伝えれば、これまでの経緯を踏まえて再開できる。
