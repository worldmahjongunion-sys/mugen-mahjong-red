# 「10ルール展開」構想への設計方針(セカンドオピニオン)

作成日: 2026-09-12
作成: Cowork(セカンドオピニオン用途)
対象: `mugen-mahjong-red`(一人麻雀アプリ、無限ルール赤入り)

---

## 経緯

Geminiとの壁打ちで、現行の「無限ルール赤入りNEO」を土台に、以下の5系統×2種=10モードへ
拡張する構想が出た。

1. 3人打ち: 無限ルールNEO(3人版) / ザンリーグルール
2. 4人打ち無限ルール: 赤入り無限ルールNEO(完成済み) / 無限ルールOriginal
3. Mリーグルール: NEO / Original
4. 麻雀共同体WWルール: スタンダード / プレミアム
5. MCR国際公式ルール: 複式MCR / MCR-NEO

Gemini側の提案は「RuleConfigオブジェクトを注入するストラテジーパターンで、コアエンジン1つに
対してルールごとのクラス/フラグを切り替える」という設計。方向性としては教科書的に正しいが、
**現行コードの実態(単一index.html、326KB、4人・136枚前提がかなり深く埋め込まれている)を
踏まえていない**ため、そのまま着手すると想定より大掛かりな改修になる可能性が高い。

以下、実際にindex.html・基準.mdを読んで確認した内容をもとに、フェーズ分けした進め方を提案する。

---

## 現状評価: コードにどれだけ「4人・136枚」が埋め込まれているか

index.html(326KB、単一ファイル)をgrepした結果:

- `const seats = ["bottom","top","left","right"]`(または並び順違い)という4人固定の配列が
  最低17箇所に直書きされている。用途は表示順・手牌公開・頭ハネ順・点差表示切り替えなど広範囲
  (例: 3323行目, 3362行目, 5237行目, 5388行目, 5409行目, 5555行目, 5627行目 など)
- 牌山生成が136枚固定でハードコード
  (1432行目コメント「136枚の牌山を作る（各種4枚ずつ）」、1446行目「34種類×4枚=136枚」)。
  王牌8枚固定も同様に直書き(1517行目付近)
- 「相手は必ず3人」という前提の割り算・分岐が18箇所以上
  - 役なしツモの精算(5100〜5110行目付近): 「3人合計」を前提に配分を計算
  - 三家和(3人同時ロン)判定(2333〜2341行目): 「3人（自分含む）が全員ロン可能」を
    決め打ちでチェック

つまり「プレイヤー数」と「牌の総数」は、RuleConfigの1プロパティとして外から差し込める形には
なっておらず、コードのあちこちに直接埋め込まれている。3人打ち(108枚・北の特殊処理)を
足すには、この17+18箇所すべてを洗い出して汎用化する作業が必要になる。

## 現状評価: 現行ルール自体、まだ「完成」ではない

基準.mdの「次のアクション」に、現行の無限ルールNEOだけでもまだ残っているタスクがある。

- 未実装の役: 海底摸月・河底撈魚・嶺上開花・槍槓・ダブルリーチ・九蓮宝燈
- 途中流局4種(九種九牌・四風連打・四家立直・四開槓/五開槓)が未実装
- 四槓子(役満)が未実装: `三槓子`判定が「3組以上」の条件になっているため、4組カンが
  揃っても四槓子ではなく三槓子3翻のまま計算されるバグ
- 対局終了時に残った供託が誰にも配分されない既知の穴
- ダブロン精算・リーチ後暗槓の猶予表示・頭ハネ順など、直近の修正がnode --checkのみで
  実機(ブラウザ)確認が済んでいない項目が複数

土台が完全に固まっていない状態で「モードを10種に増やす」という横展開を始めると、
バグの発生源が「新しいモード側の実装ミス」なのか「元々あった未消化のバグ」なのか
切り分けにくくなり、テスト配布中の知人からのフィードバックとも交錯しやすい。

---

## 推奨ロードマップ

### フェーズ0: 現行ルール(無限ルールNEO)の残タスク消化(先行推奨)

上記の未実装役・途中流局4種・四槓子バグ・供託未配分の穴を先に埋める。ここを土台にしないと、
以降どのモードを増やしても同じ穴を10倍複製することになる。

### フェーズ1(低リスク・着手しやすい): 4人打ち・136枚の前提を変えないプリセット差分

無限Original、MリーグOriginal/NEO、WWスタンダード/プレミアムの4〜5モードは、
「プレイヤー数」「牌の総数」に手を入れずに、以下のような**値レベルのRuleConfig**だけで
実現できる可能性が高い。

- 赤ドラ枚数(0/1種ずつ/現行の枚数)
- 一発・裏ドラ・カンドラの有効/無効
- MCR役(独自役満マッピング)の有効/無効
- ウマ・オカのテーブル(現行の「浮き人数で切り替え」方式 vs Mリーグ公式10-30 vs WW方式)
- ノーテン罰符の有無・金額
- 出発点・返し点

これらは既存の関数(settleYakunashi系、順位点計算部など)に「設定オブジェクトを1つ増やして
参照させる」形で対応できるはずで、17箇所の`seats`配列や136枚生成には触れずに済む。
Geminiの提案のうち、この部分(4人打ちをフラグで量産する)は妥当な指摘だと考える。

### フェーズ2(中リスク・独立スコープ推奨): 3人打ち(無限NEO三人版・ザンリーグ)

108枚化・北の抜き寄せ/客風牌としての扱い・3人固定ロジックの汎用化(前述の17+18箇所)が
必要になる。これは「フラグ1つ」では済まず、実質的に核となる進行・精算ロジックの
書き直しに近い。フェーズ1が一段落してから、独立した改修としてスコープを切ることを推奨。
無理に同じRuleConfigに「プレイヤー数」を混ぜ込もうとすると、4人打ち側(フェーズ1で
安定させたばかりの部分)に予期しない影響が及ぶリスクがある。

### フェーズ3(別モジュール推奨): MCR国際公式ルール

MCRは役の数(81種)・点数体系(88点/64点などの生点数、8点縛り)・フリテン無し・リーチ無しなど、
現行の「翻数×500+役満固定額」という評価モデルと根本的に異なる。Gemini自身も
「開発最大の難所」と認めている通り、これは**ルールのバリエーションというより別ゲーム**に近い。
既存の役判定エンジン(candidates.reduceで最大翻を選ぶ方式)に無理に統合しようとすると、
4人打ち側の安定した判定ロジックにまで影響が及びかねない。MCRは「モードの1つ」として
共通RuleConfigに含めるのではなく、独立した判定モジュール(将来的には別プロジェクトとして
切り出す選択肢も含めて)として扱うことを推奨する。

---

## 次にユーザー側で判断してほしい点

1. フェーズ0(現行ルールの残タスク消化)を先にやるか、フェーズ1(低リスクな4人打ち量産)と
   並行して進めるか
2. フェーズ1のRuleConfigに含める項目(上記の値リスト)で過不足がないか
3. 3人打ち・MCRは「いつかやる別プロジェクト」として一旦バックログに置くか、
   それとも今の時点でザックリとでも設計だけ進めておきたいか

方針が決まれば、ローカルのClaude Code CLIへの依頼文の下書きとして以下のような形が使える。

> 「基準.mdのフェーズ0未消化タスク(未実装役・途中流局4種・四槓子バグ・供託未配分)を
> 順に直してください。その後、settleYakunashi・順位点計算・ドラ処理まわりに
> RuleConfigオブジェクト(赤ドラ枚数/一発裏ドラ有無/MCR役有無/ウマオカテーブル/
> ノーテン罰符)を導入し、既存の無限ルールNEOの設定をデフォルト値として動作を変えずに
> 通す形にしてください。3人打ち・MCR関連のプレイヤー数/牌数のハードコード箇所には
> 触らないでください。」

---

## 追記(2026-09-12): ユーザー判断を受けた更新

**判断**: フェーズ0(現行ルールの残タスク)は実戦半荘20回で問題が出ておらずレアケースと
判断、フェーズ0を先行完了条件にはせず、フェーズ1(無限Original)と並行で進める方針に決定。

**無限Originalの実装コストを再調査した結果(上方修正)**:

赤ドラ(あかドラ)まわりの全処理が実際にどこから発生しているかをindex.html全体で
追跡したところ、発生源は`buildWall()`内の**1箇所だけ**であることを確認した。

```js
// index.html 1432〜1446行目付近
if(t.num === 5 && (t.suit === "man" || t.suit === "pin" || t.suit === "sou") && i === 0){
  tile.red = true;
}
```

CPU AIの赤牌温存判断(4996〜5022行目)、得点計算のakaBonus(5082〜5085行目、および
attemptWin系の各所でのakaCount集計)、見た目のハイライト(赤枠・赤い牌画像)は、
すべてこの`tile.red`フラグから導出される作りになっており、独立した「赤あり/なし」の
分岐が別途どこかに埋め込まれているわけではなかった。**つまり、この1箇所を
`if(config.akaDoraEnabled && ...)`のようにガードするだけで、赤ドラを生成しない限り
下流のCPU判断・得点計算・見た目は自動的に「赤なしルール」として整合的に振る舞う。**

前回の提案書では「フェーズ1は値レベルのRuleConfigで対応可能なはず」という見込みで
書いたが、無限Originalに限っては実際にコードを追跡して裏付けが取れた形になる
(3人打ち・MCRのような大改修とは性質が異なることが確認できた)。

**未決定・要ユーザー判断**: モードの切り替え方をどう見せるか
- (a) コード内の設定値を書き換えて別ビルドとして配る(実装コスト最小、配布の手間は増える)
- (b) 対局開始前に簡易選択画面を出す(既存の起家サイクル機能(localStorage、
  `mugenMahjongRedStartWindCycleIndex`)と同じパターンで、選択したルール名を
  localStorageに保存しておける)
- (c) index.htmlを丸ごと複製して別ファイルにする(保守が二重になるため非推奨)

(b)が一番自然だが、UIの手間が増える分だけスコープが広がる。まずは(a)で内部的に
動くことを確認してから(b)のUIを足す、という2段階に分けるのも一案。

---

## 追記2(2026-09-12): 表紙画面の仕様確定

ユーザー要望により、モード切り替えは(b)の「対局開始前の選択画面」に決定。具体的な
表紙仕様は以下の通り。

### 画面内容
- タイトル: 「AI麻雀 聖龍」
- キャッチコピー: 「現役麻雀プロが開発したAI麻雀」「10種類の麻雀ルールに対応」
- ルールボタン10個(表示順は下記):
  1. 赤入り無限ルールNEO ← **実装済み(現行のデフォルト動作)**
  2. 無限ルールOriginal ← **フェーズ1で実装予定(赤ドラ生成1箇所のガードのみ)**
  3. MリーグOriginal ← 未実装
  4. MリーグNEO ← 未実装
  5. WWスタンダード ← 未実装
  6. WWプレミアム ← 未実装
  7. 無限ルールNEO(3人打ち) ← 未実装・大改修
  8. ザンリーグルール ← 未実装・大改修
  9. 複式MCR ← 未実装・大改修
  10. MCR-NEO ← 未実装・大改修

### ボタン挙動
- 10個とも常時クリック可能な状態で表示する(グレーアウト・非表示にはしない)
- 実装済み(1・2)を押す→そのルールの対局室(既存の卓画面)に入る
- 未実装(3〜10)を押す→対局室には入らず、「工事中です、近日公開予定」という
  メッセージを表紙画面上に表示する(ブラウザ標準の`alert()`はモーダルで
  UXが硬いため非推奨。画面内にメッセージ欄を用意し、そこにテキストを出す方式を推奨)

### 実装メモ(ローカルのClaude Code CLIへの依頼で明記すべき点)
- 表紙画面(`#titleScreen`)は、既存の対局画面より手前に表示する新規のトップ画面。
  現状のアプリには表紙・メニューの概念が無い(起動直後にいきなり卓が始まる作り)ため、
  ゼロから追加する新規UI
- ルールごとの設定は`RULESETS`のような配列/オブジェクトで一元管理し、各要素に
  `id, label, status("ready"|"comingSoon"), ruleConfig`を持たせる。`ruleConfig`は
  実装済みの2つ分だけでよい(例: NEO→`{akaDoraEnabled:true}`、Original→
  `{akaDoraEnabled:false}`)
- 選択したルールIDは、既存の半荘状態保存の仕組み(`savedMatchState`
  ＝sessionStorageで局送りのreloadをまたいで保持している値)に含めて保存すること。
  含めないと、次局に進むreloadのタイミングでルールがデフォルト(NEO)に戻ってしまう
- `buildWall()`内の赤ドラ生成箇所(1432〜1446行目付近)を、選択中の`ruleConfig.akaDoraEnabled`
  を見るガード付きに変更する。これが無限Originalの実質的な差分のほぼ全て
- 【決定】既存の「リピート」「次へ」(通常時)/「リピート」「新しい半荘へ」(オーラス後、
  #resultActions)の挙動は一切変更しない。代わりに、この並びに3つ目のボタン
  「ホームページに戻る」を追加し、押すと表紙画面(`#titleScreen`)に戻る。
  通常時・オーラス後のどちらの#resultActionsにも常設する(showNormalResultActions/
  showFinalResultActionsの両方に追加する形)
  - 実装メモ: 半荘の途中(次局へがまだ残っている状態)で「ホームページに戻る」を押した
    場合、進行中の半荘を打ち切って表紙に戻ることになる。誤タップでの巻き戻り事故を
    避けたいなら、この場合だけ簡単な確認(例: window.confirm、または画面内の
    「本当に戻りますか?」の一言)を挟む案があるが、必須ではないのでローカルの
    Claude Code CLI側の判断で決めてよい
  - 「ホームページに戻る」を押した時の内部処理は、既存の「新しい半荘へ」と同じく
    clearMatchState()相当の後処理をしてから、location.reload()ではなく表紙画面を
    表示する分岐にする(表紙で選んだルールがsavedMatchStateに反映される前提のため)

---

## ローカルのClaude Code CLIへの依頼文(下書き・フェーズ1第一歩)

> mugen-mahjong-redのindex.htmlに表紙画面を追加してください。表紙は起動直後に表示し、
> タイトル「AI麻雀 聖龍」、キャッチコピー「現役麻雀プロが開発したAI麻雀」
> 「10種類の麻雀ルールに対応」、そして以下10個のルールボタンを表示します。
>
> 1. 赤入り無限ルールNEO(実装済み・現行動作) 2. 無限ルールOriginal 3. MリーグOriginal
> 4. MリーグNEO 5. WWスタンダード 6. WWプレミアム 7. 無限ルールNEO(3人打ち)
> 8. ザンリーグルール 9. 複式MCR 10. MCR-NEO
>
> 1と2以外(3〜10)はボタンを押すと対局室には入らず、「工事中です、近日公開予定」を
> 表紙画面内に表示してください(alert()は使わず画面内のメッセージ欄で)。
>
> 1(赤入り無限ルールNEO)を押すと現行動作のまま対局室に入ります。2(無限ルールOriginal)は
> buildWall()内の赤ドラ生成(1432〜1446行目付近の`tile.red = true`の分岐)を
> ruleConfig.akaDoraEnabledで判定するように変更し、Originalではfalseにしてください。
> それ以外(CPU AIの温存判断・得点計算・見た目のハイライト)はtile.red/akaCountから
> 自動的に導出される作りなので変更不要のはずです。
>
> 選択したルールIDは既存のsavedMatchState(sessionStorage)に含めて保存し、半荘途中の
> 局送りreload後もルールがデフォルトに戻らないようにしてください。
>
> 既存の結果画面のボタン(#resultActions、通常時の「リピート」「次へ」、オーラス後の
> 「リピート」「新しい半荘へ」)はそのままの挙動を残し、3つ目のボタン
> 「ホームページに戻る」を両方のパターンに追加してください。押すと表紙画面に戻ります。
> 半荘の途中で押すと進行中の半荘を打ち切ることになるので、誤タップ対策として確認を
> 挟むかどうかは実装しやすい方で構いません。
>
> 3人打ち・MCR関連のプレイヤー数(seats配列)・牌数(136枚固定)のハードコード箇所には
> 今回は一切触らないでください。

---

## 追記3(2026-09-13): 無限Originalの実際の差分が判明(想定より大きい)

ユーザーへのヒアリングの結果、無限ルールOriginalと無限ルールNEOの違いは赤ドラだけでは
なく、以下の通り実質13項目に及ぶことが判明した。実コード(index.html)を照合し、
それぞれ「値の差し替えで済むか」「新しい分岐ロジックが要るか」を判定した。

| 項目 | 無限ルールNEO(現行) | 無限ルールOriginal | 対応の性質 |
|---|---|---|---|
| 赤ドラ | あり(各色1枚) | なし | 値(buildWall 1箇所) |
| 開始点/返し点 | 35,000/40,000 | 30,000/30,000 | 値(複数箇所だが単純な差し替え) |
| ウマ・オカ | 浮き人数で3パターン切替+オカ20,000 | 固定: トップ+10,000/2,3着0/ラス−10,000、オカなし | 値(テーブル差し替え、むしろ単純化) |
| 役なし和了 | できる | できない(常に1翻縛り) | **新規ロジック**(勝ち判定にhan≥1のガードを追加) |
| 役の種類 | カスタム含む約35種 | 同じ | 変更不要 |
| 喰いタン・後付け | あり | あり | 変更不要 |
| リーチドラ | あり | あり | 変更不要 |
| 複数ロン | ダブロン(頭ハネにしない)＋邪魔ポン | 頭ハネ(単独勝者)、邪魔ポンなし | **新規ロジック**(findAllRonWinnersの結果から先頭1人だけ採用、邪魔ポンの3秒窓を無効化) |
| 王牌8枚・暗槓表示 | 現行仕様 | 同じ | 変更不要 |
| 親の連荘条件 | 上がり連荘(流局は常に親交代、`proceedOrEndMatch(false, [])`で決め打ち) | 聴牌連荘(流局時、親がテンパイなら連荘) | **新規ロジック**(流局時の連荘判定を親のテンパイ有無で分岐) |
| 本場の扱い | 親交代のたび0にリセット(`computeNextMatchState`) | リセットせず増え続ける | **新規ロジック**(親交代時の本場リセットを条件分岐に) |
| 本場ボーナス | ロン1,500/本場、ツモ各500×3人/本場 | ロン300/本場、ツモ各100×3人/本場(一般的な相場) | 値 |
| ノーテン罰符 | ノーテンの人がテンパイの人それぞれに1,000点(`settleRyuukyoku`) | 場3,000点固定プールを人数で均等割り | **新規ロジック**(計算式そのものが異なる) |

**更新した評価**: 赤ドラの時にお伝えした「1箇所だけ」という見立ては、無限Original全体には
当てはまらなかった。実際には13項目中5項目(役なし和了禁止・複数ロン方式・親の連荘条件・
本場リセット・ノーテン罰符の計算式)が値の差し替えでは済まず、新しい条件分岐が必要になる。

とはいえ、これは「別のエンジンやファイルを新たに作る」必要があるという意味ではない。
牌の判定(役の約35種・手牌分解)・4人打ち136枚のゲーム進行・CPU AI・UI表示は完全に共通の
ままでよく、上記5項目それぞれが「ruleConfigのある値を見て、既存の1〜2箇所の分岐を
追加する」レベルの変更に収まる。ただし合計すると赤ドラ1箇所だけよりは明確に規模が大きく、
このアプリがこれまで小さな変更でも複数回リグレッションを起こしてきた実績を踏まえると、
**13項目を1コミットでまとめて実装するのではなく、段階的に実装・検証することを推奨する**。

推奨する実装順序(それぞれ実機/vmハーネスでの検証を挟む):
1. 値だけで済む項目をまとめて実装(赤ドラ・開始点/返し点・ウマオカ表・本場ボーナス額)
2. 役なし和了の禁止(勝ち判定のガード追加)
3. 複数ロンの単独勝者化＋邪魔ポン無効化
4. 親の連荘条件(聴牌連荘)＋本場の非リセット化(この2つは`computeNextMatchState`/
   `proceedOrEndMatch`まわりで隣接するため同時が効率的)
5. ノーテン罰符の計算式変更

**確認事項**: 供託(リーチ棒)の扱い(その局でリーチして和了した者だけが受け取る、
半荘終了時に残っていたらトップの取り分)はNEOと同じという理解で進めるが、
違いがあれば教えてほしい。

### 追加のヒアリング内容(同日)

さらに以下の項目が判明。表を更新する。

| 項目 | 無限ルールNEO(現行) | 無限ルールOriginal | 対応の性質 |
|---|---|---|---|
| 飛びによる即終了 | あり(`checkMatchEnd`が"bust"を検出し即終了) | なし | 値/条件の削除(checkMatchEndの"bust"分岐をスキップするだけ) |
| 70,000点超えのコールド終了 | あり | なし | 値/条件の削除(同、"over70000"分岐をスキップ) |
| 途中流局(九種九牌・四風連打等) | **NEOでも未実装**(基準.mdの残タスク) | なし(=NEOと同じく実装しない) | **変更不要**(両方とも無いままでよい) |
| W役満(複数役満の重ね取り) | **NEOも現状は単一候補の最大値選択方式で、役満は常に固定額**(重ね取りは元々発生しない) | なし | **変更不要**(現状の実装のままで既に条件を満たしている) |
| 流し役満 | **NEOでも未実装** | なし | **変更不要** |

このヒアリングで、当初懸念した項目のうち3つ(途中流局・W役満・流し役満)は
「NEOに元々無い機能をOriginalでも作らない」という話に過ぎず、実装コストはゼロと判明した。
飛び・コールド終了の2つも、既存の`checkMatchEnd`内の分岐を無効化するだけなので低リスク。
新規ロジックが必要な項目は変わらず5つ(役なし和了禁止・複数ロン単独勝者化・親の連荘条件・
本場非リセット・ノーテン罰符計算式)のまま。

**新たな確認事項**: 「飛び終了なし」ということは、対局が終わらないまま半荘の最後まで
マイナス点を持ち越せる、という理解でよいか。その場合、現行NEOにある「飛ばした人が
和了者に10,000点を払う」という精算ルール自体は、Originalではどうなるか(①同じく
0点を割った瞬間に10,000点だけ精算して対局は続行する、②精算自体も無く単にマイナス点の
まま計算を続ける、のどちらか)を教えてほしい。

### 追加のヒアリング内容(2026-09-13 その2)

さらに2項目、以下が判明。いずれもコードを確認したところ、既存の変数・計算結果を
そのまま流用できるため、想定よりずっと簡単に実装できることが分かった。

| 項目 | 無限ルールNEO(現行) | 無限ルールOriginal | 対応の性質 |
|---|---|---|---|
| 一発 | ロン・ツモどちらでも付く | 出和了り(ロン)のみ、自摸では付かない | **新規ロジックだが低リスク**: 一発判定箇所(4017行目・4570行目の2箇所、`if(riichiDeclared[seat] && ippatsuActive[seat])`)に`isTsumo`条件を追加するだけ。`isTsumo`は同じスコープに既に存在する |
| 七対子の4枚使い | 可(同じ牌4枚を2対子として七対子成立、緑一色四枚使い等の役満もこれを利用) | 不可(7種類がそれぞれちょうど2枚ずつでなければ七対子として認めない) | **新規ロジックだが低リスク**: 「7種類が全部ちょうど2枚」という条件は、実は連七対の判定(4540行目`keys.length===7 && keys.every(k=>counts[k]===2)`)として既に計算済み。Originalではこの条件をそのまま七対子成立の必須条件に追加すればよい。4枚使い前提の役満(緑一色四枚使い等)はOriginalでは自動的に不成立になる(標準の七対子2翻、または不成立で他の分解を試みる) |

これで新規ロジックが必要な項目は合計7つ(役なし和了禁止・複数ロン単独勝者化・親の連荘条件・
本場非リセット・ノーテン罰符計算式・一発ツモ無効化・七対子4枚使い禁止)になったが、
後者2つは既存の変数・計算結果の流用で済むため、実装コストへの影響は小さい。

### 飛び終了・飛び賞の最終回答(2026-09-13)

「飛び終了なし・マイナス点はそのまま最後まで持ち越す・飛び賞(10,000点)も無し」と確定。
これはむしろNEOより単純になる変更で、Originalでは以下を丸ごとスキップすればよい。

- `checkMatchEnd`の"bust"分岐(0点未満で即終了) → スキップ(実装済みの通り)
- 飛び精算(飛ばした人が和了者に10,000点払う処理一式) → 丸ごと未使用
- 支払いを0点で止める「0点止め」処理(現行NEOは支払いでマイナスにならないようキャップしている) → Originalは外し、通常の点数移動をそのままマイナスまで許可する

NEO側の「0点止め」や「飛び精算」のコード自体には触れず、Original実行時だけこれらの
分岐を通らないようにする(ruleConfig.tobiEnabled=falseのような1フラグで、該当処理群を
まとめて迂回させる形が良い)。

---

## 最終確定仕様(まとめ): 無限ルールNEO vs 無限ルールOriginal

これまでのヒアリングで出た差分の全項目。以降の依頼文はこの表を正としてローカルの
Claude Code CLIに渡す想定(これより前の版の依頼文は、この表に統合されたため参考情報)。

| # | 項目 | NEO(現行) | Original | 対応 |
|---|---|---|---|---|
| 1 | 赤ドラ | あり(各色1枚) | なし | 値 |
| 2 | 開始点/返し点 | 35,000/40,000 | 30,000/30,000 | 値 |
| 3 | ウマ・オカ | 浮き人数別3パターン+オカ20,000 | 固定(トップ+10,000/2,3着0/ラス−10,000)、オカなし | 値 |
| 4 | 本場ボーナス | ロン1,500/本場、ツモ各500×3人/本場 | ロン300/本場、ツモ各100×3人/本場 | 値 |
| 5 | 役なし和了 | 可 | 不可(常に1翻縛り) | 新規ロジック |
| 6 | 複数ロン | ダブロン(頭ハネにしない)+邪魔ポン | 頭ハネの単独勝者、邪魔ポンなし | 新規ロジック |
| 7 | 親の連荘条件 | 上がり連荘(流局は常に親交代) | 聴牌連荘(流局時、親がテンパイなら連荘) | 新規ロジック |
| 8 | 本場のリセット | 親交代のたび0にリセット | リセットせず増え続ける | 新規ロジック |
| 9 | ノーテン罰符 | ノーテン1人がテンパイ1人に1,000点(人数分乗算) | 場3,000点固定プールを均等割り | 新規ロジック |
| 10 | 一発 | ロン・ツモどちらも付く | ロン(出和了り)のみ | 新規ロジック(低リスク、isTsumoの流用) |
| 11 | 七対子4枚使い | 可 | 不可(7種類ちょうど2枚ずつのみ) | 新規ロジック(低リスク、既存の連七対判定条件の流用) |
| 12 | 飛びによる即終了 | あり | なし | 条件を無効化するだけ |
| 13 | 70,000点コールド終了 | あり | なし | 条件を無効化するだけ |
| 14 | 飛び賞(10,000点)・0点止め | あり | なし(マイナス点をそのまま持ち越す) | 該当処理一式を丸ごとスキップ |
| 15 | 途中流局・W役満・流し役満 | 未実装(NEOにも無い) | 同じく無し | 変更不要 |
| 16 | 喰いタン後付け・リーチドラ・王牌8枚固定・暗槓中国式伏せ | あり | 同じ | 変更不要 |
| 17 | 役の種類(約35種) | 実装済み | 同じ | 変更不要 |
| 18 | 供託(リーチ棒)の扱い | 和了者が受取、残りはトップへ | 同じという前提で進行(要最終確認) | 変更不要(前提) |

## ローカルのClaude Code CLIへの依頼文(最終版・これ以前の依頼文を差し替え)

> mugen-mahjong-redのindex.htmlに、既存の「赤入り無限ルールNEO」に加えて
> 「無限ルールOriginal」を実装してください。両ルールはこのファイル内の
> 「最終確定仕様(まとめ)」の表(項目1〜18)の通りに差分があります。表紙画面
> (`#titleScreen`)は実装済みなので、ボタン2「無限ルールOriginal」を選んだ時だけ
> Original側のruleConfigが使われるようにしてください。
>
> 実装は1コミットにまとめず、次の順序で段階ごとにコミット・検証してください。
> 1. 値の差し替えのみの項目(1〜4、12〜14): ruleConfigに`akaDoraEnabled`
>    `startingPoints``returnPoints``umaTable``okaEnabled``honbaRonBonus`
>    `honbaTsumoBonusPerSeat``earlyEndOnBust``earlyEndOnOver70000``tobiEnabled`
>    のようなフィールドを追加し、Original用の値を設定。NEO用のデフォルト値は
>    現行の動作と完全に一致させること(既存のNEOプレイに影響が出ないことを最優先)
> 2. 役なし和了の禁止(項目5): 和了判定でhan===0(役なし)の場合、Originalでは
>    和了を成立させない
> 3. 複数ロンの単独勝者化+邪魔ポン無効化(項目6): `findAllRonWinners`の結果から
>    Originalでは先頭1人だけを採用し、邪魔ポンの3秒割り込み窓を発生させない
> 4. 親の連荘条件+本場リセット(項目7・8): 流局時の連荘判定を、Originalでは
>    親がテンパイかどうかで決める。`computeNextMatchState`のrenchan=false分岐で、
>    Originalでは本場をリセットしない
> 5. ノーテン罰符の計算式変更(項目9): `settleRyuukyoku`をOriginal用に、場3,000点を
>    テンパイ人数で均等に受け取り、ノーテン人数で均等に支払う方式に変更
> 6. 一発ツモ無効化(項目10)・七対子4枚使い禁止(項目11): それぞれ既存の`isTsumo`・
>    連七対判定の条件を流用して追加
>
> 各段階の後、既存のNEOルールで一切のリグレッションが無いこと(node --checkと
> 可能な範囲のvmハーネス検証)を確認してから次の段階に進んでください。
>
> 3人打ち・MCR関連のプレイヤー数(seats配列)・牌数(136枚固定)のハードコード箇所には
> 今回は一切触らないでください。

---

## 追記4(2026-09-14): 実戦テストで発見したバグ — 河底撈魚・海底摸月未実装の実害化

無限Originalの実戦テストで、「海底(河底撈魚)以外に役が無い手」が和了拒否される事象が
発生。原因を特定: 河底撈魚・海底摸月は基準.mdに元々あった未実装項目で、NEOでは
「役なし和了」が救済していたため実害が無かったが、Originalの「常に1翻縛り
(役なし和了禁止、項目5)」により、この手が正真正銘「役なし」として和了そのものを
拒否されてしまった。

**対処**: 河底撈魚(ロン)・海底摸月(ツモ)を実際の役として実装する。NEO・Original
どちらにも有効な修正(NEOでも本来もらえるはずの1翻が付いていなかった)。

> (ローカルのClaude Code CLIへの追加依頼)
> 河底撈魚・海底摸月を役として実装してください。山が尽きた後の最後のツモ
> (`wall.length===0`になった直後のツモ)でツモ和了した場合は海底摸月(1翻)、
> その人が続けて打牌した牌(それ以降誰もツモれない最後の捨て牌)でロン和了した場合は
> 河底撈魚(1翻)を付与してください。NEO・Original両方に適用し、Originalの
> 「役なし和了禁止」判定より前に、この役の付与判定が済んでいることを確認してください。

---

## 追記5(2026-09-14): 画面遷移・成績管理・3人打ち系の詳細仕様(Geminiからの追加設計案の検証)

Geminiから追加の設計案が来た。画面遷移(ホーム→ルール別ページ→対局)の詳細、3人打ち系
(無限NEO三人版/ザンリーグ)・MCR系(複式/NEO)の中身、クラス設計案、無料/有料版の料金体系案
が含まれる。実コード(index.html、343KB、直近ステージ済み)と照合し、以下の通り評価する。

なお、以前依頼していた確認事項(`RULESETS`配列に`id:"akaNeo"`/`id:"original"`が
それぞれ1箇所ずつしか無いか)は、実ファイルを直接読んで確認済み。重複は無かった。

### 画面構成の追加(低〜中リスク)

- ホーム画面のボタンを「横2列×縦5列」の固定グリッドで表示: 低リスク。現状の表紙画面CSSは
  `grid-template-columns:repeat(auto-fit, minmax(200px, 1fr))`(自動折り返し、1071行目)に
  なっている。`repeat(2, 1fr)`のような固定2列に変える程度の変更で済む。
- ボタンの表示順が今回の案では「①3人打ち→②4人打ち→③Mリーグ→④WW→⑤MCR」(3人打ちが先頭)。
  現在実装済みの`RULESETS`配列の並びは「赤入り無限NEO→無限Original→Mリーグ×2→WW×2→
  3人打ち×2→MCR×2」(4人打ちが先頭)。単純な配列なので並び替え自体はいつでも低コストで
  できる。順序をどちらにするかは未決定のまま置いておいて問題ない(変えたくなったら一言で対応可能)。
- 各ルールに「ルール別ホーム画面」(README/グラフ/成績/対局/ホームに戻る の5ボタン)を
  挟む案は、現状のアプリに無い**新しいナビゲーション階層**。今は表紙のボタンを押すと直接
  対局室(卓)に入る作りなので、「表紙→ルール別ホーム→対局室」という中間層をまるごと
  追加することになる。表紙画面の仕組み(`#titleScreen`)と同じパターンを再利用できるので
  技術難易度自体は高くないが、10ルール分×5ボタンぶん作業量は増える。

### 「成績管理」「直近10戦のグラフ」は新規の永続化機能(このアプリで一番大きい新規要素)

index.htmlを確認したところ、現状は**対局中の1半荘分の状態(savedMatchState、
sessionStorage)しか保持しておらず、半荘が終わった後の記録を残す仕組みは一切ない**
(grepで「成績」「history」「matchHistory」に該当する実装は実質ゼロ件)。

つまり以下は、赤ドラ1箇所ガードのような軽い変更ではなく、**このアプリに初めて
「対局の記録を長期保存する」という概念を持ち込む新機能**になる。

- トップ率・連対率・和了率・放銃率・副露率・リーチ率(MCRのみ自摸率)・平均得点・
  平均順位・平均和了点・平均放銃点 → 半荘ごと・局ごとの結果を逐一記録し集計する仕組みが要る
- 直近10戦の折れ線グラフ → 上記の記録から直近10件を取り出して描画。single-file構成を
  崩さないなら外部グラフライブラリではなく素のcanvas/SVGで自前描画が現実的

設計としては、ルールIDごとに`localStorage`へ「半荘終了時の結果を1件追記」という形の
記録を持たせ(例: `mugenMahjongRedMatchHistory_<ruleId>`というキーに配列でpush)、
対局終了処理(現行のshowFinalStandings相当)のタイミングでフックする形が既存構造に
一番馴染む。ただし「和了率・放銃率・副露率・リーチ率」は半荘単位ではなく**局単位**の
集計なので、局が終わるたびに発生するイベント(和了した/放銃した/鳴いた/リーチした)を
新たに拾う必要がある。現状のコードは局ごとの結果を画面表示はしているが、それを
構造化データとして貯め続ける仕組みはまだ無い。

この「成績管理基盤」は10ルール共通で使い回せる部分が大きい(ルールIDをキーにするだけ)
ので、10種類それぞれに作るのではなく、先に1つの共通の記録・集計エンジンを作ってから、
各ルールの画面はそれを呼び出すだけにするのが良いと考える。この基盤づくりは、無限Original
実装より少し大きめの独立したフェーズとして切り出すことを推奨する(フェーズ1.5、くらいの
位置づけ)。

### 3人打ち: 追加情報の確認(既存の「大改修」評価がさらに補強された)

今回、「無限ルール(3人打ち)は北を抜きドラとして刻子で一飜、使用する牌(萬子2〜8抜き)を
制御する」という具体的な仕様が判明。以前から指摘していた「108枚化・北の特殊処理・
3人固定ロジック(17+18箇所)」に加えて、**牌山の構成そのもの(34種類→27種類、萬子2〜8を
除く)を変える**という追加の変更点になる。`buildWall()`は現状「34種類×4枚=136枚」を
固定で生成しており(1432〜1446行目)、萬子の一部を除外する分岐は存在しない。フェーズ2
(3人打ち)の評価は「中リスク・独立スコープ推奨」のまま変わらないが、想定される作業範囲は
これで一段と明確になった。

### ザンリーグ: 花牌の実装コストは訂正(下方修正)、祝儀・青金牌は引き続き未確認

ユーザーから花牌の実体について確認が取れた: **花牌は独立した第5の牌種ではなく、
五萬の牌4枚すべてを赤く塗って代用したもの**で、手牌の中では使えない「抜きドラ」
(ツモった瞬間に手牌から抜いて場に晒し、代わりに山から1枚補充ツモする)として扱われる。

これは同じ3人打ちグループで検討中の「北抜きドラ」(ツモったら手牌から抜いて場に晒し、
王牌から補充ツモする、いわゆるカン抜きと同じ仕組み)と**技術的には全く同じ仕組み**
(抜き取り→即補充ツモ、というイベント自体は麻雀の進行フローに新しい分岐を1つ足すだけ)。
違うのはどの牌を対象にするか(北 vs 赤五萬)と、抜いた牌がもたらすボーナスの中身
(北抜きドラは刻子ボーナス等/花牌はザンリーグ独自の祝儀点)だけなので、**北抜きドラの
仕組みを一度作れば、花牌はその仕組みに「対象牌の指定を変える」形でほぼそのまま乗せられる**
可能性が高い。前回「花牌=新しい牌種の追加で重い」とした評価は訂正し、実装コストは
3人打ちの北抜きドラと同程度(まとめて1つの「抜きドラ機構」として実装すれば足りる)に
下方修正する。

**「赤・青・金牌の扱い」も確認済み**: 色による祝儀点の違いは無く、花牌代用の4枚は
すべて赤(五萬を赤く塗ったもの4枚のみ)とのこと。前段の評価(北抜きドラと同じ仕組みで
実装可能)がそのまま当てはまる。

**新たに判明: 「白ポッチ」という追加の特殊牌が1枚ある**。白(白ドラ/字牌の白)のうち
1枚だけ特別な印(ポッチ)が付いており、通常は白として通常通り使える(刻子・対子など)が、
**聴牌後にこの牌を自摸ると、待ち牌の中から一番点数が高くなる牌として扱って和了できる
(オールマイティ牌としての「高め取り」)**、という追加ルール。

これは花牌(抜きドラ)とは別系統の仕組みで、実装として新たに以下の性質を持つ。
- 白4枚のうち1枚だけを特別な牌として区別する必要がある(tile.red的な、tile.wild
  というフラグを1枚にだけ立てるイメージ)
- 通常の手牌判定(刻子・対子等)では普通の白と全く同じに扱ってよい
- 「聴牌中にこの牌を自摸った時だけ」和了判定のトリガーになり、かつ「今の待ち牌の候補の
  中から一番高い点数のものとして扱う」という、既存の役判定エンジンの「候補から最大翻を
  選ぶ(candidates.reduceで最大翻を選ぶ方式)」の考え方をそのまま使えそうな性質を持つ

**確認済み**: 前者(現在の待ち牌の中から一番点数が高い牌を自摸ったことにして計算する)
で確定。「既に手の内にある間はオールマイティ牌として機能しない、聴牌完成後にこの牌を
自摸った瞬間だけ、その時の待ち牌候補の中から最高点になる牌として和了計算する」という
仕様。これはコストが小さい側の解釈で、実装は次のように整理できる。

- 通常時(手牌に入っている間・鳴き・打牌時): 普通の白と完全に同一に扱う(区別不要)
- 聴牌中にこの牌を自摸った時だけ: 現在の待ち牌一覧(machi)を求め、それぞれの待ち牌で
  和了した場合の得点を既存の役判定関数でそれぞれ計算し、最高得点のものを採用する
- 「白のうちどの1枚が特殊牌か」を1枚だけ区別するフラグ(tile.red・tile.wild的なもの)
  さえ持たせれば、既存の「候補から最大翻を選ぶ」エンジンをほぼそのまま呼び出す形で
  対応できる見込み。新しい役判定ロジックを書く必要はなく、既存の待ち牌計算・役判定
  関数を「待ち牌の数だけループして呼ぶ」程度の変更で済むはず

あわせて未確認のまま残っている点:
- 祝儀(チップ)の具体的な計算方法(誰が誰に払うか、上限、役満時の扱い等)
- 「独自のスピード感」が具体的に何を指すか(時間制限、発声ルール等)

**「ツモ損なし」は確認済み**: 「本来支払うべき人が支払う点数を、残りの二人が折半する方法」
とのこと。つまり、通常の「親倍・子1倍」というツモの配分ではなく、まず**ロンだった場合に
本来支払うはずの1人分の点数(ロン基準の満額)を算出し、その金額を残りの二人(3人打ちなので
相手は2人)で折半して支払う**。ツモでもロンと同じ総取り点を確保でき、通常のツモ配分に
ありがちな端数の切り上げによる「ツモ損」が発生しない、という仕組みと理解した。

これは現行コードのツモ精算(dealer2倍・non-dealer1倍を各自fuで端数処理する方式、
computeTsumoAmounts相当)とは配分ロジックそのものが異なるため、「値の差し替え」ではなく
**新規の分岐(ロン基準額を算出→2等分、という専用の計算式)**が必要。ただし複雑な新機構
ではなく、既存のロン計算関数を呼んでから2で割るだけなので、実装コスト自体は小さいと見る。

これらが分かれば、ザンリーグの評価(現時点では「3人打ちの中では祝儀計算があるぶん
やや重い」程度に見えるが、確定は保留)を更新する。

### Mリーグ・WW: フェーズ1の想定通り(低リスク)

Mリーグ(Original=公式ルール完全再現、NEO=そこから1要素だけ変える)・WW(スタンダード=
一発裏ドラカンドラノーテン罰符なしの競技ルール、プレミアム=マイル計算や順位ウマの差)は、
いずれも「4人打ち・136枚」の前提を変えずに済む値レベルの差分に見える。無限Original実装時
にRuleConfigへ追加したフィールド(`akaDoraEnabled`等)に加えて、`uraDoraEnabled`・
`kanDoraEnabled`(WW用、現状は両方とも常時有効でOFFにする分岐が無い)を足す程度で
対応できる可能性が高い。既存の評価(フェーズ1・低リスク)を維持する。

### MCR: 複式MCRはMCR-NEOよりさらに1段階重い(トーナメント形式そのものが新機能)

MCR-NEO(単卓・素点重視+順位点少々)は、点数体系こそ別エンジンが要るものの、卓の構造
自体は現行と同じ「自分+CPU3人・1卓完結」で収まる。一方、複式MCR(CPU4種A/B/C/Dを作り、
チームを組んで4卓を同時進行、卓ごとにメンバーが入れ替わる総当たり方式)は、**複数卓を
同時にシミュレートしてチームスコアを集計する、現行アプリに存在しない「大会・トーナモード」**
そのものであり、MCR-NEOの役・点数エンジンができて初めて着手できる、その上のレイヤー。
フェーズ3の中でもMCR-NEO→複式MCRの順で、複式MCRはさらに先(フェーズ3b、くらいの
位置づけ)に置くことを推奨する。

### クラス設計案(BaseMahjongEngine階層)について

「3人打ちベース/4人打ちベース/MCR専用ベースに分けてルールをサブクラス化する」という
設計案は、**方向性としては今までの提案(3人打ちは独立スコープ、MCRは別モジュール)と
一致しており、違和感はない**。ただし注意点として、現行のindex.htmlは340KB超の単一
スクリプトで、クラス継承ではなく素の関数群+グローバルな状態変数(seats配列、wall配列等)
で書かれている。この図の通りに実際にクラス階層へ書き直すのは、それ自体が大規模な
リファクタリングになり、無限Originalのような「既存関数にRuleConfigを1個足す」対応とは
規模が違う。**同じ分離の効果(3人打ち専用処理と4人打ち専用処理が混ざらない)は、必ずしも
クラス継承を導入しなくても、現行の関数ベースのままruleConfig・playerCountで分岐する形で
十分に実現できる**。本格的にクラス階層へ書き直す価値があるとすれば、MCR(本当に別ゲームに
近い)くらいで、そこも「別モジュール」程度の切り出しで足りると考える。

### 無料版/有料版・ネット対戦について: これはルール拡張とは別の意思決定

無料版/有料版1/有料版2の料金体系、月額・年額プラン、ネット対戦(時間帯指定の大会・
ONE DAY大会・リーグ戦)は、**10ルール展開そのものとは別軸の意思決定**として切り分ける
ことを勧める。現行のmugen-mahjong-redは「ビルドもサーバーも無い単一HTMLファイル」であり、
課金・アカウント管理・リアルタイム対戦を実現するには、決済処理・ユーザー認証・サーバー
(リアルタイム通信を含む)が新たに必要になる。これは今回のRuleConfig拡張やUI画面追加とは
性質もスケールも全く異なる、**別プロジェクトとして扱うべき領域**。10ルールの土台が
固まってから、独立して検討することを推奨する(このタイミングで一緒くたに設計を進めると、
ルール拡張自体の見通しが悪くなる)。

### 次にユーザー側で判断してほしい点

1. ホームボタンの並び順を「3人打ち先頭」に変えるか、現状の「4人打ち先頭」のままにするか
2. 「成績管理基盤」(フェーズ1.5)を、Mリーグ・WWの実装(フェーズ1残り)より先にやるか後にやるか
3. 無料版/有料版・ネット対戦は、今回の10ルール展開のスコープから完全に外して
   「いつか別途検討する話」として置いておいてよいか

---

## 追記6(2026-09-14): ザンリーグ公式競技規定(簡易版)を受領 — 独立したチップ精算エンジンが必要

ユーザーから「ザンリーグ | 三人麻雀競技団体」の公式競技規定(簡易版)の全文が提供された。
これまでの断片的な情報(花牌・白ポッチ・ツモ損なし)を含む、完全な公式ルールブック。
以降このルールに関する依頼はこの原文を正として扱う。

### 原文(参照用に保存)

> **スコアについて**: 数字によるスコアではなくチップの枚数で競う。半荘終了時、素点を
> 万点台の区分に当てはめ、下表で2着・3着のチップ授受を決める(1着は2着・3着の増減の
> 合計を反転した分を受け取るゼロサム構造と推定)。
>
> | 素点帯 | 2着 | 3着 |
> |---|---|---|
> | 9万点台 | +8枚 | |
> | 8万点台 | +6枚 | |
> | 7万点台 | +4枚 | |
> | 6万点台 | +2枚 | |
> | 5万点台 | 0枚 | -6枚 |
> | 4万点台 | -2枚 | -8枚 |
> | 3万点台 | -4枚 | -10枚 |
> | 2万点台 | -6枚 | -12枚 |
> | 1万点台 | -8枚 | -14枚 |
> | 千点台 | -10枚 | -16枚 |
> | 0点 | -12枚 | -18枚 |
>
> **チップについて**: 一発・裏ドラ・立直後白ポッチツモに各+1枚。飛ばした人に+2枚
> (ダブロンは分け)。役満祝儀はロン10枚・ツモ5枚オール。華4和了(花牌代用牌を4枚とも
> 抜いた)は役満祝儀(打点はドラ4扱い)。
>
> **基本ルール**: 5万点持ち(3-3-5)、500点棒(100点棒なし)。親は聴牌連荘。喰いタン・
> 後付けあり、ツモ平和あり。ピンズとソウズの5は全て赤ドラ(4枚とも)。華牌あり(強制抜き、
> 空気扱い)。白ポッチあり(リーチ後永久ツモ扱い)。ドラ表示の横までツモりきり。北は全員の
> 役牌。ケイテンあり、純カラも聴牌扱い。途中流局なし(5回目のカン不可)。リーチ後の見逃し
> なし。リーチ後のカンは任意(待ちが変わらなければ可)。オープンリーチは通常リーチと同じく
> 1000点供託で可。ツモ番のないリーチ可。ノーテン罰符は場に2,000点。積み棒はロン1,000点・
> ツモ1,000点オール。持ち点1,000点でのリーチ可。トビ終了あり(0点は飛び終了)。ダブロン有
> (供託・積み棒・親権は上家取り)。国士無双の暗槓和了あり。ダブル役満・パオなし。数え役満に
> 祝儀は無し。同点時の着順は上家取り。トップの点数に関わらず西入なし。トップ時のみ和了やめ・
> 聴牌やめが任意で可。ドラ表示が出てこない場合は親が自山の左から5番目をめくる。符計算なし
> (固定点表、下記)。
>
> **親和了(ロン/ツモオール)**: 1翻2000/1000、2翻3000/2000、3翻6000/3000、4翻12000/6000、
> 6翻18000/9000、8翻24000/12000、11翻36000/18000、13翻48000/24000
>
> **子和了(ロン/ツモ子・ツモ親)**: 1翻1000/1000、2翻2000/1000、3翻4000/1000・3000、
> 4翻8000/3000・5000、6翻12000/4000・8000、8翻16000/6000・10000、11翻24000/8000・16000、
> 13翻32000/12000・20000
>
> **特殊役**: 七対子4枚使い可。オープンリーチ1翻(手牌全開け)。三連刻2翻。三風2翻。
> 小車輪6翻(混一色七対子)。流し役満(鳴いたり鳴かれたら終了)。人和役満。四連刻役満。
> 大車輪役満(清一色七対子)。萬子混一色役満。
>
> **修正可能**(下家の打牌前までのリーチ取消=アガリ放棄、捨て牌一段目までの少牌補充=
> ノーペナ)/**ノーペナルティ**(見せ牌・腰牌・アガリ牌誤って手に入れた後の和了=安め取り・
> 白ポッチ誤打=戻してアガリ 等、多数)/**アガリ放棄**(多牌・少牌・オープンリーチのアガリ牌
> 誤打・誤ポン誤ロン誤ツモの発声のみ 等)/**チョンボ**(誤ロン誤ツモの倒牌・ノーテンリーチ
> 発覚・リーチ後待ちの変わるカン・山崩し・フリテンリーチ発覚・見逃し発覚 等、
> 半荘終了時にチップ10枚没収・その局はノーゲーム)/**役満払い**(オープンリーチへの
> 手詰まり押し出しは点棒のみ)。

### Cowork側の評価

**新たに実コードで確認できたこと(2点、評価に直結)**:
- 国士無双は既に13翻の役満として実装済み(4750〜4763行目、13種の牌キーを個別に判定する
  方式)。ただし現状のロジックは「手牌(門前の13/14枚)」を対象にしており、暗槓した牌は
  別の面子データ構造に移る作りのため、**「国士無双の暗槓和了あり」を素直に成立させるには、
  暗槓済みの牌もこの13種判定に含める小さな修正が必要**(新規ロジックだが範囲は狭い)
- リーチ後の暗槓制限は`canAnkanAfterRiichi(seat, tile)`として既に実装済み(基準.mdの
  修正履歴より、「待ちが変わらなければ可」という条件も含めて対応済み)。**つまり
  「リーチ後のカンは任意(待ちが変わらなければ可)」は、無限NEO/Originalと全く同じ既存
  ロジックがそのまま使え、ザンリーグ用に何も新しく作る必要が無い**

**北の扱いについて訂正**: 以前「3人打ちは北が抜きドラ」と記載したが、これは無限ルール
(3人打ち)側の話であり、**ザンリーグでは北は抜きドラではなく「全員が使える役牌」**
(誰が北を刻子にしても役牌1翻になる、抜き取りは発生しない)。ザンリーグの北まわりは
むしろ単純な値レベルの対応(役牌判定リストに北を追加するだけ)で済む。抜きドラの仕組みは
無限ルール(3人打ち)側でだけ必要、という整理に訂正する。

**赤ドラの配置が現行と根本的に違う**: 現行(NEO/Original共通)は「萬子・筒子・索子それぞれ
5の1枚」が赤ドラだが、ザンリーグは「萬子の5は花牌代用のため使えず、代わりにピンズ・
ソウズの5は4枚とも(1枚だけでなく全部)赤」という配置。`buildWall()`の赤マーキング条件
(`i===0`、各色5の最初の1枚だけ)を、ザンリーグでは「萬子5は対象外、ピンズ・ソウズ5は
`i`条件を外して4枚とも赤」に変える必要がある。値レベルだが、既存の条件式そのものを
ルール別に分岐させる書き方が要る。

**新規に追加が要る主なもの(まとめ)**:
1. **チップ精算エンジン**: 半荘終了後、素点の万点帯からチップ授受表を引く新規の後処理。
   一発・裏ドラ・白ポッチツモ・飛び・役満・華4和了それぞれの「+チップ」イベントを
   局ごとに記録する仕組みも合わせて必要(成績管理基盤(追記5)と同様、「対局の記録を
   残す」という新しい概念が要る点は共通)
2. **特殊役9種の追加**: オープンリーチ・三連刻・三風・小車輪・流し・人和・四連刻・大車輪・
   萬子混一色。萬子混一色が役満格なのは、萬子5が花牌代用で減っているため萬子清一色/混一色
   の難度が上がっている埋め合わせと推測され、理にかなっている
3. **オープンリーチ**: 手牌を公開した状態でのリーチ。+1翻の役として実装するだけなら
   小さいが、実際に手牌を見せるUI(CPU側に手の内が見える演出)まで作るかは別途判断が要る
4. **ダブロン時の供託・積み棒・親権の扱い**: 「上家取り」は、既存の`headBumpOrder`
   (頭ハネ順を決める関数)がそのまま使える。ダブロンで複数人が和了しても、供託・積み棒・
   連荘権だけは`headBumpOrder`の先頭者に寄せる、という小さな追加分岐で済む
5. **数え役満とチップ対象外の区別**: 役満チップ(ロン10枚・ツモ5枚オール)は、名前のある
   役満(国士無双・大車輪等)にのみ適用し、通常役の合算で13翻に達した「数え役満」には
   適用しない。既存のyaku配列に「これは名前つき役満かどうか」の目印が要る
6. **5回目のカン不可の上限チェック**: 途中流局(四開槓)を実装しない代わりに、
   「4回カンが出た後の5回目のカンは成立させない」という上限チェックが新規に要る
   (現状カン回数の総数を追跡する仕組みがあるか要確認)
7. **トップ時のみ和了やめ・聴牌やめが任意で可**: 最終局・トップ確定時などに現れる
   特殊な選択肢で、UIに新しい分岐ボタンが要る。頻度は低いが完全に新規

**値の差し替えで済むもの(まとめ)**: 5万点持ち(3-3-5)・500点棒、親聴牌連荘(Original実装
と同じ仕組み)、喰いタン後付け・ツモ平和(現行と同じ)、七対子4枚使い可(NEOと同じ、
Originalとは逆)、ノーテン罰符場2,000点(Originalのpool方式の値違い)、積み棒ロン1,000・
ツモ1,000オール、トビ終了あり0点終了(NEOのtobiEnabled=trueと同じ仕組み)、西入なし
(4人打ちのNan入り判定と同様、3人打ち側で対応)

**大部分が対象外/低優先と判断したもの**: 「修正可能」「ノーペナルティ」「チョンボ」
「役満払い」の一覧は、対人卓での誤操作・誤発声への対応ルール。相手がCPU3人(このアプリの
構造)である以上、UI側でそもそも誤ポン・誤ロン・先ヅモ等が起こらない作りにする方が
近道で、これらを麻雀ルールとして机械的に実装する必要性は薄いと考える。同様に「ドラ表示が
出てこない場合は親が…」は物理卓限定の手順であり、デジタル実装では常に次のドラ表示が
一意に決まるため対象外。「リーチ後の見逃しなし」も、CPU戦では見逃し自体が(UIが自動で
ロン可否を判定・提示する作りである限り)発生しにくく、対象外に近いと考える。

**残っている細かい確認事項**(急ぎではない):
- 固定点表が1・2・3・4・6・8・11・13翻の行しか無い(5・7・9・10・12翻が無い)。一般的な
  「5翻=満貫、6〜7翻=跳満、8〜10翻=倍満、11〜12翻=三倍満、13翻〜=役満」という帯に
  対応していると推測し、その前提で実装を進めてよいか
- 「白ポッチあり(リーチ後永久ツモ扱い)」という表現から、白ポッチのオールマイティ機能は
  リーチ後に限定される(まだリーチしていない門前聴牌・食い聴牌では発動しない)という
  理解でよいか(前回のヒアリングでは「聴牌している状態で」とだけ確認していたため)
- 「ツモ番のないリーチ可」「ケイテンあり、純カラも聴牌扱い」は、恐らく現状の実装が
  すでに満たしている(何もしなくてよい)可能性が高いが、念のため実装時に確認してほしい

---

## 追記7(2026-09-14): Mリーグ・WWの公式ルールをWeb検索で調査(要ユーザー確認)

ローカルのClaude Code CLIから「Mリーグ・WWも無限Originalの時のような正確な数値表が
欲しい」との要望があった。これはコード側の裏取りではなく実在の競技団体の公式ルールの
話なので、Web検索で一次情報に近いものを調べた。**ただし以下はあくまで公開情報からの
調査結果であり、白石さんご自身の方が正確にご存知の可能性が高いので、必ず確認・訂正の上
でローカルのClaude Code CLIに渡してほしい。**

### Mリーグ(注意: Mリーグ運営は完全な公式ルール全文を一般公開していない)

参照した記事(雀天)自体が「Mリーグ公式が競技ルールの全文を一般公開していないため、
Wikipedia・MLスタジアム高田馬場のルールpage等の二次資料が典拠」と明記している。
つまり以下は**二次情報**であり、シーズンによる改定がある可能性も含め、精度は
無限Originalの時ほど高くない。

| 項目 | 内容(二次情報) |
|---|---|
| 持ち点/返し点 | 25,000持ち/30,000返し |
| オカ | 20,000点(1着総取り) |
| ウマ | +30/+10/-10/-30(オカ込みだと1着は実質+50) |
| 赤ドラ | 3枚(萬子・筒子・索子の5が各1枚、現行NEOと同じ配置) |
| 一発 | あり |
| 裏ドラ | あり |
| カンドラ | 明記無し(恐らくあり) |
| ノーテン罰符 | **記事に記載無し**(不明、要確認) |
| トビ終了 | **無し** |
| コールド終了 | 記載無し(恐らく無し) |
| ダブロン | **無し**(頭ハネ、放銃者の下家側から順に判定) |
| 喰いタン・後付け | あり |
| 流し満貫 | 無し |
| 途中流局(九種九牌等) | 無し |
| 切り上げ満貫 | あり(30符4翻・60符3翻→満貫) |
| 数え役満 | **無し**(11翻以上は三倍満止め、本物の役満手のみ役満扱い) |
| 役満の複合(ダブル役満) | **あり**と明記されている(要注意、下記) |
| パオ(責任払い) | 大三元・大四喜・四槓子に適用 |
| 本場 | 1本場+300点 |
| チョンボ | 点数の授受は無く、順位点から一律▲20.0(打点ペナルティでは無い方式) |

**要注意**: 「役満の複合(ダブル役満)あり」は、現行アプリのエンジン(候補から最大翻を
1つだけ選ぶ方式、複数役満が重なっても単一の役満固定額のまま)と根本的に異なる。
Mリーグ Originalを本当に完全再現するなら、複数役満が重なった時に金額を積み増す
ロジックが新規に要る(これは「値の差し替え」では済まない、Mリーグ固有の新規ロジック)。
シンプルに倒すなら、ここだけ現行の「単一役満固定額」のまま据え置く簡略化も選択肢。

**Mリーグ NEOについて**: 「Originalに1要素だけ変える」という当初の設計意図の通り、
どの1要素を変えるかはCowork側では決められない(白石さんの意匠)。決まれば表に追加する。

### 麻雀共同体WW(公式サイトで確認: スタンダードとプレミアムの関係が当初の想定と逆だった)

公式サイト(mcww2015.com)の/ruleページには実際には5種類のルールリンクがある:
四人打ち(プロリーグ)・四人打ち(スタンダード)・四人打ち(東風戦)・四人打ち(プレミアム)・
三人打ち。**アプリのRULESETSに既にある「wwStandard」「wwPremium」は、この
スタンダードとプレミアムにそのまま対応させられる。**

**訂正**: 以前(追記5)「スタンダード=一発裏ドラカンドラノーテン罰符なしの競技ルール」と
記載したが、公式サイトで確認したところ**逆だった**。一発・裏ドラ・カンドラが
無いのはプレミアムの方。

| 項目 | スタンダード | プレミアム |
|---|---|---|
| 持ち点/返し点 | 30,000/30,000(オカ無し) | 30,000/30,000(オカ無し) |
| 満貫 | 切り上げ満貫あり | 切り上げ満貫**無し**(30符6翻でも子7700・親11600の実数計算) |
| ウマ | 10-30(同点は均分) | **5-15**(同点は均分、スタンダードよりウマが小さい) |
| 一発・裏ドラ・カンドラ・カン裏 | **あり** | **無し** |
| 赤ドラ | 無し | 無し |
| 喰いタン・後付け | あり | あり |
| 食い替え | 不可 | スジ食い替え可・現物食い替え不可 |
| フリテン | (標準通り) | ロン以外の制約無し(スタンダードより緩い) |
| ダブロン・トリプルロン | 無し | (記載箇所からは同様に無しと推測) |
| テンパイ連荘 | あり(1本場+300点) | 記載箇所からは同様と推測 |
| トビ | **無し**。0点以下になったら卓内トップ者から10000点単位で借りる、という独自の処理 | 同様の記載 |
| チョンボ | 半荘終了時に順位点から▲20 | 同様 |
| パオ | 大三元・大四喜・四槓子に適用 | 同様 |
| ノーテン罰符 | **公式ページに記載無し**(不明、要確認) |

「マイル計算」というプレミアムの特徴は、公式ルールページのどこにも見当たらなかった。
麻雀共同体WWの何らかの会員向けポイント制度(大会参加特典等)であり、対局中の点数計算
ルールとは別の話ではないかと推測する。**在籍中の白石さんの方がご存知だと思うので、
実在するルールなら教えてほしい。事実で無ければ、このアプリのWWプレミアムには
反映しないでおく。**

### 要確認事項(まとめ)

1. Mリーグの上記数値(特にノーテン罰符・カンドラ)は合っているか
2. Mリーグ Originalで「役満の複合」を本当に実装するか、それとも現行同様に単一役満
   固定額のまま簡略化するか
3. Mリーグ NEOで変える「1要素」は何にするか
4. WWの上記数値(特にノーテン罰符)は合っているか
5. WWの「0点以下は卓内トップ者から10000点単位で借りる」処理を、このアプリでは
   どう扱うか(半荘終了時の精算に影響する実際の点数移動として実装するか、
   単に「マイナス表示のまま進行してよい」と簡略化してよいか)
6. 「マイル計算」は実在する制度か、対局中の点数ルールとして反映すべきものか

---

## 追記8(2026-09-14): Mリーグ・WWの数値確定、およびMリーグNEOが「値1つ」では済まないと判明

追記7の要確認事項にユーザーから回答があり、Mリーグ・WWの数値はほぼ確定した。一方で
Mリーグ NEOの「変える1要素」は、当初の想定(値をひとつ変えるだけ)を大きく超える、
アプリの根幹に関わる新機構だと判明した。

### Mリーグ Original: 確定

- ノーテン罰符: 場3,000点(Original/ザンリーグと同じ「固定プール均等割り」方式、
  `notenPenaltyMode:"pool", notenPenaltyPool:3000`)
- カンドラ: あり(確認済み。現行の裏ドラ・カンドラの仕組みがそのまま使える前提)
- 本場ボーナス: 標準ルールと同じロン300/本場・ツモ各100/本場(現行Originalの値と同一、
  追加確認は不要と判断)
- ウマ・オカ: 追記7の通り、順位ウマ+30/+10/-10/-30、オカ20,000(1着総取り)。
  現行の`fixedUmaTable`方式に合わせて逆算すると、`fixedUmaTable:[50000, 10000, -10000, -30000]`
  (1着だけ「ウマ+30,000+オカ20,000」を合算した値)にすれば、`score - returnPoints(30000)`
  との合計がちょうどゼロサムになる
- 親の連荘・本場リセット・七対子4枚使い: 標準的な現代麻雀のルールに合わせ、Original側
  (`tenpaiRenchanEnabled:true`・`honbaResetOnDealerChange:true`・
  `chiitoitsuFourTileEnabled:false`)と同じ設定で問題ないと判断
- **新規: 役満の複合(W役満・トリプル役満・4倍役満)を実装する**。ユーザーからの明確な
  指示で、例として「天和+大三元+四暗刻+字一色の4倍役満」が挙げられた。現行エンジンは
  役満を含め常に「候補から最大1つだけ選ぶ」方式(複数の役満条件を満たしても単一の
  固定額のまま)なので、これは値の差し替えでは済まない**新規ロジック**。和了判定で
  「成立している役満をすべて列挙し、その数だけ基準額(現行の役満固定額、例: ロン48000)
  を掛け算する」という変更が必要。Mリーグ Original・NEO共通で必要になる(NEOはOriginalの
  ルールを土台にするため)

### WW(スタンダード・プレミアム): 確定

- ノーテン罰符: **両方とも場3,000点**(固定プール均等割り、Mリーグ・Originalと同じ方式)
- 箱点(0点未満)以下: マイナス表示のまま進行してよい、点棒の貸し借り(公式ルールにある
  「卓内トップ者から10,000点単位で借りる」処理)は**実装しない**。無限Original・
  ザンリーグと同じ「マイナスのまま持ち越す」扱いでよい、と明確な指示
- プレミアムの点数: 切り上げ満貫にせず、子7700・親11600(実数)と確定。追記7のWeb調査
  結果と一致
- スタンダード/プレミアムの実像(参考情報): スタンダードは最高位戦日本プロ麻雀協会の
  一発裏ドラありルートと同種、プレミアムは日本プロ麻雀連盟Aルールに近く、
  「順位点5-15」「食い変えあり(現物食い替えは不可)」が主な違い、との位置づけをユーザーから
  確認。実装上の値は追記7の表(ウマ10-30/5-15、一発裏ドラカンドラの有無等)をそのまま使う

**新たに判明した設計上の懸念(要判断)**: WWの点数(子7700・親11600、切り上げ満貫の有無)は、
いずれも**符(fu)を使った本来の麻雀の点数計算**に基づく値。しかし現行アプリの得点エンジンは
「符計算をせず、翻数×500+役満固定額」という簡略モデル(ザンリーグの節で既出の通り、
アプリ全体で符計算が存在しない)。つまりWWを額面通りに再現するには、符計算(面前ロン+10符・
ツモ+2符・待ちの形・刻子/槓子の種類と門前/鳴きによる符・雀頭による符 等)をゼロから
実装する必要があり、**これは「値の差し替え」ではなく、このアプリに無い新しい計算体系を
まるごと追加すること**に相当する(規模感としてはMCRの点数エンジンに次ぐ大きさ)。

これを避ける現実的な代案は、ザンリーグと同様に「符計算はせず、翻数だけを見た固定点表
(WW用の翻数→点数テーブルを別途用意する)」で近似すること。切り上げ満貫の境界(30符4翻等)
のような符依存の細かい挙動は再現できなくなるが、実装コストは大きく下がり、既存アプリの
設計思想(符計算なし)とも一致する。**個人の練習用アプリとしての用途を考えると、この
簡略化(翻数だけの固定点表)を推奨するが、最終判断は白石さんにお任せしたい。**

### Mリーグ NEO:「少牌+オールマイティー」は値1つの変更ではなく、アプリの根幹に関わる新機構

ユーザーから詳細な説明があり、当初「Originalに1要素だけ変える」という想定だった
Mリーグ NEOは、実際には**手牌の持ち方そのものを変える**、かなり本格的な新機構だと
判明した。

**ルールの内容**: 各人は配牌時に(通常13枚ではなく)**12枚の実牌+架空のオールマイティー
牌1枚**を持った状態で対局を開始する。この架空の牌はどんな牌としても扱える万能牌で、
実際の牌山からは引かれない(牌山の総枚数には影響しない、純粋に概念上のボーナス牌)。
結果として、本来なら「一向聴(イーシャンテン)」だった配牌が、常に「聴牌」として扱われる。

**このルールが生む効果(ユーザー説明の通り)**: イーシャンテン地獄が無くなる/流局が
極端に減る/高い手が出やすい/多面待ちになりやすく一発が出やすい/役満も出やすい/
一方でフリテンにもなりやすい/多面待ちが多いため先行リーチが有利で、親のリーチには
向かいにくい、という独特のスリリングな麻雀になる。追加ルールとして「オープンリーチ」
(ザンリーグで既出のもの)のみ別途採用する。ベース自体はMリーグ Originalのルール
(ウマオカ・ノーテン罰符・役満複合等、上記の確定値を含む)を丸ごと引き継ぐ。

**Cowork側の評価**: これは「RuleConfigの値を1つ変える」話ではなく、**アプリの手牌の
持ち方そのものを変える構造変更**で、影響範囲が非常に広い。

- 配牌枚数が13枚→12枚(親は14→13)に変わるため、牌山からの配牌ロジック・残り牌山枚数の
  計算(海底/河底のタイミングにも影響)を専用に作り直す必要がある
- 「架空の万能牌」は、シャンテン計算・聴牌判定・待ち牌計算・役判定・フリテン判定の
  **すべての場面で常時存在する13枚目**として扱わねばならない。これは、ザンリーグの
  「白ポッチ」(和了の瞬間、今の待ち牌の中からだけ最高点を選ぶ、という限定的な適用)
  よりもずっと影響範囲が広い、**「常に34種類のどれとしても機能しうる牌が手牌に
  含まれている」という前提で、シャンテン・聴牌・待ち・役・フリテンの各計算を作り直す**
  必要がある本格的な万能牌
- CPU(相手3人)も同じ12枚+万能牌でプレイするため、**CPU AIの打牌判断ロジックも
  この特殊な手牌構成を前提に作り直す**必要がある(現行のCPU AIは13/14枚の通常手牌を
  前提にしている)
- 「オープンリーチ」の追加は、ザンリーグで検討したものと同じ仕組みを流用できる

**結論として、Mリーグ NEOはMリーグ Original・WWとは全く別格の規模になる。**
無理に同じバッチで実装しようとすると、他の低リスクな項目まで巻き込んで長期化する
リスクがあるため、**Mリーグ Original・WWスタンダード・WWプレミアムの3つを先に
実装し、Mリーグ NEOはザンリーグと同様「独立した設計フェーズ」として別途スコープを
切ることを推奨する。**(最終判断は白石さんにお任せする)

### ローカルのClaude Code CLIへの依頼文(下書き・Mリーグ Original + WW2種のみ)

> mugen-mahjong-redのindex.htmlに、Mリーグ Original・WWスタンダード・WWプレミアムの
> 3ルールを追加してください(Mリーグ NEOは今回含めません、別途スコープします)。
> `RULESETS`配列の該当エントリ(`mLeagueOriginal`・`wwStandard`・`wwPremium`)の
> `status`を`"ready"`に変更し、以下の`ruleConfig`を設定してください。
>
> **Mリーグ Original**: `akaDoraEnabled:true`(現行と同じ配置)、`startingPoints:25000`、
> `returnPoints:30000`、`umaMode:"fixed"`、`fixedUmaTable:[50000,10000,-10000,-30000]`、
> `honbaRonBonus:300`、`honbaTsumoBonusPerSeat:100`、`earlyEndOnBust:false`、
> `earlyEndOnOver70000:false`、`tobiEnabled:false`、`yakunashiWinEnabled:false`、
> `multiRonEnabled:false`、`jamaPonEnabled:false`、`tenpaiRenchanEnabled:true`、
> `honbaResetOnDealerChange:true`、`notenPenaltyMode:"pool"`、`notenPenaltyPool:3000`、
> `ippatsuOnTsumoEnabled:true`、`chiitoitsuFourTileEnabled:false`。加えて、和了判定で
> 成立している役満をすべて列挙し、その数だけ基準の役満固定額を掛け算する
> 「役満複合」ロジックを追加してください(単一役満固定額のみを返す現行ロジックからの変更)。
>
> **WWスタンダード**: `akaDoraEnabled:false`、`startingPoints:30000`、`returnPoints:30000`、
> `umaMode:"fixed"`、`fixedUmaTable:[30000,10000,-10000,-30000]`、一発・裏ドラ・カンドラ
> あり、`tobiEnabled:false`(0点未満はマイナス表示のまま進行、貸し借り処理は実装しない)、
> `notenPenaltyMode:"pool"`、`notenPenaltyPool:3000`。点数は符計算をせず、翻数だけの
> 固定点表(標準的な切り上げ満貫込みの値)で近似してください。
>
> **WWプレミアム**: WWスタンダードと同じ基本値で、`fixedUmaTable:[15000,5000,-5000,-15000]`、
> 一発・裏ドラ・カンドラは**無効**にしてください。点数は切り上げ満貫にせず、
> 子7700・親11600(4翻30符相当)を固定値として使う翻数別の点数表にしてください
> (符計算はせず、翻数だけの固定点表で構いません)。
>
> 実装は他のルール追加と同様、値の差し替え部分から段階的にコミット・検証してください。
> Mリーグ NEO(少牌+オールマイティー牌)は今回のスコープに含めないでください。

---

## 追記9(2026-09-14): 符計算の話はMリーグにも及ぶと判明。標準の点数早見表を確定

ユーザーから訂正: 符計算に基づく点数は**WW固有の懸念ではなく、Mリーグも同じ仕組み**
(実在の麻雀の符計算に基づく点数体系)とのこと。つまり追記8で「WWだけ簡略化するか」と
書いたのは対象が狭すぎた。**Mリーグ Original・NEOとWW スタンダード・プレミアムの
4つすべてが、現行アプリに存在しない「符計算に基づく実在の点数」を必要とする**、
という理解に訂正する。

Web検索で標準的な麻雀の点数早見表(30符換算)を確認したので、追記8で提案した
「符計算はせず、翻数だけの固定点表で近似する」簡略化案を、実際に使える表として
以下に確定する。**切り上げ満貫の有無(Mリーグ/WWスタンダード=切り上げあり、
WWプレミアム=切り上げ無し)は、4翻の行の値だけが変わる形になる。**

### 固定点表(切り上げ満貫あり: Mリーグ Original・NEO、WWスタンダードで共通使用)

| 翻数 | 子ロン | 親ロン | 子ツモ(非親/親) | 親ツモ(オール) |
|---|---|---|---|---|
| 1翻 | 1,000 | 1,500 | 300 / 500 | 500 |
| 2翻 | 2,000 | 3,900 | 500 / 1,000 | 1,000 |
| 3翻 | 3,900 | 5,800 | 1,000 / 2,000 | 2,000 |
| 4翻 | 満貫 8,000 | 満貫 12,000 | 2,000 / 4,000 | 4,000 |
| 5翻(満貫) | 8,000 | 12,000 | 2,000 / 4,000 | 4,000 |
| 6〜7翻(跳満) | 12,000 | 18,000 | 3,000 / 6,000 | 6,000 |
| 8〜10翻(倍満) | 16,000 | 24,000 | 4,000 / 8,000 | 8,000 |
| 11〜12翻(三倍満) | 24,000 | 36,000 | 6,000 / 12,000 | 12,000 |
| 13翻〜(役満) | 32,000 | 48,000 | 8,000 / 16,000 | 16,000 |

(Mリーグは前述の通り、この表の値を役満の数だけ倍加する「役満複合」ロジックを
別途適用する)

### 固定点表(切り上げ満貫なし: WWプレミアムのみ)

上表と全く同じだが、**4翻の行だけ**以下の実数値に差し替える(5翻以降は満貫〜役満の
固定バンドなので、切り上げの有無に関係なく元々同じ値)。

| 翻数 | 子ロン | 親ロン | 子ツモ(非親/親) | 親ツモ(オール) |
|---|---|---|---|---|
| 4翻 | 7,700 | 11,600 | 2,000 / 3,900 | 3,900 |

### 実装メモ

現行アプリの得点関数(`computeRonAmount`・`computeTsumoAmounts`等)は「翻数×500」という
連続的な計算式のため、上記のような「翻数ごとに不連続な値が並ぶルックアップテーブル」
とは構造が異なる。Mリーグ・WW用には、翻数をキーにした配列/オブジェクト(例:
`SCORE_TABLE_KIRIAGE[han]`・`SCORE_TABLE_NO_KIRIAGE[han]`、5翻以降は同じ値を埋める
か「5翻以上はmin(han,13)を跳満以降のバンドに丸める」処理)を新設し、既存の
`akaBonus`・`honbaBonus`等の加算部分だけ既存ロジックを再利用する形が現実的。

この表はあくまで「符計算をしない前提の近似値」であり、実際の符(面前ロン+10符・
ツモ+2符・待ちの形・刻子の種類等)によって多少前後する実在の点数とは、細かい部分
(特に2翻30符=2000 vs 40符2翻=2600等、fu違いによる差)で一致しない場合がある。
個人の練習用アプリとしての精度で十分と考えるが、この前提でよいか念のため確認したい。

---

## 追記10(2026-09-14): Mリーグ Original・WWスタンダード・WWプレミアム実装後のリリース前レビュー

コミット済みの実ファイル(350KB版、`RULESETS`配列・`SCORE_TABLE_KIRIAGE`/
`resolveScoreHanAndMultiplier`等)を直接読んで確認した。全体として設計方針書の
表と一致しており、実装品質は良好。以下、リリース前に判断・確認してほしい点を
2つに絞ってフラグを立てる。

### 1. 要修正の可能性あり: WWの「七対子4枚使い」がtrueになっている

`chiitoitsuFourTileEnabled`が、Mリーグ Originalでは`false`(依頼文で明記した通り)
だが、**WWスタンダード・WWプレミアムはどちらも`true`になっている**。これは
Cowork側の依頼文にこの項目を書き忘れたための、ローカルのClaude Code CLI側の
デフォルト値(恐らくNEOの設定を引き継いだもの)であり、意図した値ではない可能性が高い。

実在の競技ルール(最高位戦・プロ連盟Aルール等、WWが参考にしている団体)は、
一般に七対子の4枚使い(同じ牌4枚を2対子として扱う)を認めていないのが標準的。
Mリーグ Originalを`false`にしたのと同じ理由で、**WWの2つも`false`にする方が
実態に合っていると考える。** ただし公式サイトのルールページにこの項目の明記は
無かったため、断定はできない。修正するかどうか判断してほしい。

### 2. 判断待ち: カンドラ未実装(ローカルのClaude Code CLI側から提起済み)

ローカルのClaude Code CLIから提起された通り、現行エンジンには「カンをするたびに
ドラ表示が1枚増える」仕組み自体が無く(あるのは常時表向きの1枚のドラ表示と、
リーチ時のみ有効になる裏ドラだけ)、Mリーグ Original・WWスタンダードの両方で
本来必要な「カンドラ」を実装しないまま`ready`にしている
(コード内コメントで明記済み、隠された手抜きではない)。

これは追記9で確認済みの「符計算をしない近似」と同種の**個人利用アプリとしての
簡略化**として片付けることもできるし、別途カンドラを実装する
(カンのたびに新しい表ドラ表示を1枚追加する仕組みを新設)こともできる。
実装コスト自体は「表ドラ表示を複数枚保持できるようにする」程度で、カン発生
頻度を考えると影響は限定的とみられるが、優先度を上げるかどうかは白石さんの
判断に委ねたい。

### その他(問題なし、確認のみ)

- 役満複合ロジック(`resolveScoreHanAndMultiplier`)を実際に読んだところ、
  名の付いた役満(han≥13)をyaku配列から個別にカウントし、その数だけ倍率をかける
  実装になっており、複数役満が同時成立するケース(例: 大三元+字一色)でも
  正しく2倍・3倍になる作り。数え役満の三倍満止めも、名の付いた役満が0個の場合
  だけhan12(三倍満)に丸める形で正しく実装されている
- 一発の`ippatsuEnabled`(WWプレミアムで一発自体を無効化)と
  `ippatsuOnTsumoEnabled`(無限Originalでロンのみ有効化)の2つのフラグは、
  両方とも正しく併用されており(`ippatsuEnabled && (ippatsuOnTsumoEnabled || !isTsumo)`)、
  意図通りに動作する
- 赤ドラは全ルール共通で「翻数には影響しない固定点数ボーナス」として扱われており
  (実在のMリーグ・WWでは赤ドラも通常のドラと同様に翻数へ加算されるのが本来の
  ルール)、これはMリーグ・WW固有の新しい簡略化ではなく、**アプリ全体が元々
  持っていた設計(赤ドラ=翻ではなく固定点)をそのまま踏襲しているだけ**。
  今回の新規劣化ではないため、緊急の修正は不要と判断

## 追記11: 七対子4枚使いの最終確定 + カンドラの具体的な実装方針(コード裏付け済み)

### 1. 七対子4枚使い: 全ルール共通パターンとして確定

白石さんから明確な回答あり:

> Mリーグルール・MリーグNEOルールとも、カン毎にドラが増えます、WWのスタンダード
> ルールも同様です。カンはドラ扱いで１ハン増えます。七対子の4枚使いは無しです。
> 七対子の4枚使OKなのは三人打ちとMCR。赤入り無限ルールNEOだけです。

これで追記10の判断待ち事項(1)がクローズする。**`wwStandard`・`wwPremium`両方とも
`chiitoitsuFourTileEnabled: false`に修正が必要**(現状は誤って`true`のまま)。

あわせて、このアプリ全体を貫く「七対子4枚使い」の最終パターンが確定したので、
今後追加する残りのルール(3人打ちNEO・ザンリーグ・複式MCR・MCR-NEO)にもこのまま
適用できるよう明文化しておく:

| ルール | `chiitoitsuFourTileEnabled` |
|---|---|
| 赤入り無限ルールNEO | `true`(既存・変更なし) |
| 無限Original | `false`(実装済み・変更なし) |
| Mリーグ Original / NEO | `false`(Originalは実装済み、正しい値) |
| WWスタンダード / プレミアム | **`false`に修正**(現状`true`は誤り) |
| 3人打ちNEO(無限ルールNEO 3人打ち)・ザンリーグ | `true`(未実装、実装時にこの値で) |
| 複式MCR・MCR-NEO | `true`(未実装、実装時にこの値で) |

### 2. カンドラ: 現行コードを読んで具体的な実装方針を確定

ローカルのClaude Code CLIが自己申告した「複数枚の表ドラを保持する仕組みが無い」という
点について、実ファイル(350KB版index.html)を直接読み、正確な現状構造を確認した。

**現状のドラ表示牌まわりの構造(コード裏付け):**

```js
// 1760行目付近
const deadWall = wall.splice(0, 8);
// 王牌8枚固定の内訳(コード内コメントより):
//   [0]         = 表ドラ表示牌(doraIndicator、常時公開)
//   [1]         = リーチドラ表示牌(riichiDoraIndicator、裏ドラ相当)
//   [2],[3]     = 予備2枚(現状どこからも参照されない未使用領域)
//   [4]〜[7]    = 嶺上牌(末尾からpop()して使う。4枚=カン4回分の上限)
const doraIndicator = deadWall[0];        // 単一値
const riichiDoraIndicator = deadWall[1];  // 単一値
```

```js
// 4750行目付近: ドラ枚数の集計も「表示牌1枚ぶん」しか見ていない
function countDoraDetail(allTiles){
  const doraTile = nextTileForDora(doraIndicator);
  return allTiles.filter(t => t.suit === doraTile.suit && t.num === doraTile.num).length;
}
function countRiichiDoraDetail(allTiles){
  const riichiDoraTile = nextTileForDora(riichiDoraIndicator);
  return allTiles.filter(t => t.suit === riichiDoraTile.suit && t.num === riichiDoraTile.num).length;
}
```

`rinshanAvailable()`は`deadWall.length > 4`でカン可否を判定しており(4枚使い切ったら
カン不可)、`LIVE_WALL_TOTAL`・海底摸月/河底撈魚の`wall.length === 0`判定はどちらも
`wall.length`を都度動的に読んでいるだけで、王牌の枚数を固定値としてハードコードしている
箇所は無い。つまり**王牌の総枚数を8枚から増やしても、既存ルール(赤入り無限NEO・
無限Original・ザンリーグ等)の山ツモ回数計算や海底判定は自動的に追随し、壊れない**
ことが確認できた。これがカンドラを実装する上での一番のリスク(山の残り枚数の
つじつまが合わなくなること)だったので、ここが安全だと分かったのは大きい。

**推奨する実装方針(`ruleConfig.kanDoraEnabled: true`のルール限定で分岐):**

1. `kanDoraEnabled`ルール(Mリーグ Original・Mリーグ NEO・WWスタンダードの3つのみ。
   WWプレミアムは元々一発・裏ドラ・カンドラが無い設計なので対象外、無限系・
   3人打ち系・MCR系も対象外)の時だけ、王牌を8枚ではなく14枚切り出す
   (`wall.splice(0, ruleConfig.kanDoraEnabled ? 14 : 8)`)。本家麻雀の王牌14枚
   (嶺上4+表ドラ最大5+裏ドラ最大5)と同じ内訳にする:
   `[0]`=表ドラ1枚目、`[1]`=裏ドラ1枚目、`[2]〜[5]`=表ドラ2〜5枚目の予備、
   `[6]〜[9]`=裏ドラ2〜5枚目の予備、`[10]〜[13]`=嶺上牌4枚(従来通り末尾からpop)。
2. `doraIndicator`/`riichiDoraIndicator`の単一値を、`doraIndicators`/
   `riichiDoraIndicators`という配列(最初は各1枚で開始)に変更する。
3. カンが成立する4箇所(`executeAnkan`・`executeCpuAnkan`・`executeKakan`、および
   大明槓成立処理)それぞれで、`ruleConfig.kanDoraEnabled`の時だけ予備領域から
   次の1枚を`doraIndicators`・`riichiDoraIndicators`にpushする(カン成立のたびに
   表ドラ・裏ドラを1枚ずつ同時に追加。カン最大4回なので予備4枚×2系統で足りる)。
4. `countDoraDetail`/`countRiichiDoraDetail`を、単一の`nextTileForDora(indicator)`
   1回判定から、配列の各要素についてそれぞれ判定して合算する形に変更する
   (`addDoraYaku`側の「ドラ」「リーチドラ」という役名・翻数加算の扱いは変更不要。
   白石さんの言う通り「カンドラも普通のドラと同じ1ハン加算」なので、既存の
   ドラ役の集計先を配列合算にするだけで新しい役カテゴリは不要)。
5. 王牌の見た目表示(1184行目付近のコメントにある「3枚目=表ドラ表示牌、
   4枚目=リーチドラ表示牌」という固定4枠のUI)は、カンが発生した対局でのみ
   表示牌が増えるため、追加分の見た目対応は最小限のテキスト表示(または
   既存の牌絵を横に追加)で済ませる形を想定。得点計算の正しさが最優先で、
   王牌演出の見栄えは後回しでも実害はない。

**リスク評価:** 設定フラグ1つの差し替えでは済まない、正味の新規実装。ただし
影響範囲は「王牌の切り出し枚数」「ドラ表示牌を単一値→配列に変える3関数」
「カン成立4箇所への1行追加」に限定でき、当初懸念していた「山の残り枚数の
つじつま」は上記の通り問題にならないと確認済み。実装後はMリーグ Originalの
対局で実際にカンを発生させ、表ドラが2枚(以上)になった状態で和了して翻数が
正しく増えることを手動確認するのが望ましい。

### ローカルのClaude Code CLIへの依頼文(下書き)

> 以下2点の修正をお願いします。
>
> **① 七対子4枚使いの修正(値の差し替えのみ)**
> `wwStandard`・`wwPremium`の`ruleConfig.chiitoitsuFourTileEnabled`を、
> 現状の`true`から`false`に修正してください(Mリーグ Originalと同じ値。
> 白石さんが実際のルールとして確認済みです)。
>
> **② カンドラの実装(Mリーグ Original・WWスタンダードの2つに適用。
> WWプレミアムは対象外、Mリーグ NEOは着手時期未定のため今回は対象外)**
> 現行エンジンは表ドラ表示牌(`doraIndicator`)・リーチドラ表示牌
> (`riichiDoraIndicator`)を単一値として保持しており(1760〜1765行目付近、
> 王牌8枚固定)、カンのたびに表ドラ表示が1枚増える「カンドラ」の仕組みが
> ありません。これを新しいruleConfigフラグ`kanDoraEnabled`(Mリーグ Original・
> WWスタンダードで`true`、他は`false`)で分岐し、`true`の時だけ王牌を14枚
> (本家の内訳=嶺上4+表ドラ予備5+裏ドラ予備5)に増やし、`doraIndicator`/
> `riichiDoraIndicator`を配列化して、カン成立のたびに表ドラ・裏ドラを1枚ずつ
> 追加する形にしてください。ドラ枚数の集計(`countDoraDetail`/
> `countRiichiDoraDetail`)も配列の全要素を合算する形に変更してください
> (役自体は既存の「ドラ」「リーチドラ」のままで、翻数への加算の仕方も
> 変更不要です)。`kanDoraEnabled:false`の既存ルール(赤入り無限NEO・
> 無限Original・WWプレミアム等)は王牌8枚・単一表示牌のまま一切変更しないでください。

## 追記12: カンドラ実装(コミットbdbe5c3・242e130)の実コードレビュー結果

ローカルのClaude Code CLIが追記11の依頼2点を実装完了、実ファイル(353,579バイト版
index.html)を再取得して直接コードを読み、レビューした。

**結論: 実装は正しい。設計通りで、CLI自身が作業中に見つけて直した2件のバグ
(`rinshanAvailable()`と`updateRinshanVisual()`が旧8枚固定の王牌サイズを
ハードコードしたままで、カンドラ有効ルールでは王牌が14枚に増えているのに
閾値がずれる/カン回数上限が効かなくなる不具合)も、共通定数
`DEAD_WALL_INITIAL_SIZE`に一本化する形で正しく修正されている。**

確認した具体的な実装(コード裏付け):

- `chiitoitsuFourTileEnabled`: `wwStandard`・`wwPremium`とも`false`に修正済み
  (1645・1661・1674行目)。`sanninNeo`(3人打ち、未実装)は元々`true`のまま。
- `kanDoraEnabled`: `mLeagueOriginal`・`wwStandard`のみ`true`、`akaNeo`・
  `original`・`wwPremium`は`false`(1620・1633・1646・1662・1675行目)。
  設計通りWWプレミアムは対象外のまま
- 王牌: `DEAD_WALL_INITIAL_SIZE = ruleConfig.kanDoraEnabled ? 14 : 8`で分岐
  (1765〜1766行目)。`doraIndicators`/`riichiDoraIndicators`が配列化され
  (1771〜1772行目)、`revealNextKanDora()`がカン成立時に`deadWall[2+kanIndex]`
  (表ドラ予備)・`deadWall[6+kanIndex]`(裏ドラ予備)を1枚ずつpushする形
  (1773〜1780行目)。カン成立4箇所(大明槓・自分の暗槓・CPU暗槓・加槓)全てで
  `revealNextKanDora()`呼び出しを確認(3078・3261・3475・3505行目)
- `rinshanAvailable()`は`deadWall.length > DEAD_WALL_INITIAL_SIZE - 4`に修正
  (2518行目)、`updateRinshanVisual()`も`DEAD_WALL_INITIAL_SIZE - deadWall.length`
  に修正(1947行目)。旧ハードコード値`8`は残っていない
- `countDoraDetail`/`countRiichiDoraDetail`は`doraIndicators`/
  `riichiDoraIndicators`の全要素を`reduce`で合算する形に修正済み(4778〜4792行目)。
  単一値時代の`doraIndicator`/`riichiDoraIndicator`という変数名の使い残しは
  全ファイル検索で見当たらず(該当するのはHTML要素id `doraIndicatorTile`等の
  無関係な文字列一致のみ)
- CPU AIの「見えている牌・ドラ温存判断」ロジック(5261〜5284行目)も
  `doraIndicators.forEach`/`riichiDoraIndicators.forEach`に更新済みで、
  カンドラ追加後もCPUの判断材料に反映される

**唯一の(バグではなく)既知の簡略化点:** 王牌の見た目表示(1186行目のUI・
6383〜6387行目のpaintTile)は、追記11で提案した通り「1枚目の表示牌だけを
描画し続ける」設計のまま(コード内コメントで明記済み)。つまりカンドラ有効
ルールで2枚目以降の表ドラが追加公開されても、卓の見た目には反映されない
(得点計算には正しく反映される)。プレイヤーが「何巡目のカンで追加ドラが
乗ったか」を画面上で確認する手段が無いという点だけ、見た目の後回しとして
残っている。得点自体は正しいため急ぎの修正は不要だが、気になるようであれば
今後の見た目改修のバックログに追加できる。

以上でMリーグ Original・WWスタンダード・WWプレミアムの3ルールは、追記10・11で
指摘した2点とも解消・実装確認済みとなった。

