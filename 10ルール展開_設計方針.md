# 「10ルール展開」構想への設計方針(セカンドオピニオン)

作成日: 2026-09-12
作成: Cowork(セカンドオピニオン用途)
対象: `mugen-mahjong-red`(一人麻雀アプリ、無限ルール赤入り)

---

## 経緯

Geminiとの壁打ちで、現行の「無限ルール赤入りNEO」を土台に、以下の5系統×2種=10モードへ
拡張する構想が出た。

1. 3人打ち: 無限ルールNEO(3人版) / ザンリーグルール
2. 4人打ち無限ルール: 赤入り無限ルールNEO(完成済み) / 無限ルールOriginal
3. Mリーグルール: NEO / Original
4. 麻雀共同体WWルール: スタンダード / プレミアム
5. MCR国際公式ルール: 複式MCR / MCR-NEO

Gemini側の提案は「RuleConfigオブジェクトを注入するストラテジーパターンで、コアエンジン1つに
対してルールごとのクラス/フラグを切り替える」という設計。方向性としては教科書的に正しいが、
**現行コードの実態(単一index.html、326KB、4人・136枚前提がかなり深く埋め込まれている)を
踏まえていない**ため、そのまま着手すると想定より大掛かりな改修になる可能性が高い。

以下、実際にindex.html・基準.mdを読んで確認した内容をもとに、フェーズ分けした進め方を提案する。

---

## 現状評価: コードにどれだけ「4人・136枚」が埋め込まれているか

index.html(326KB、単一ファイル)をgrepした結果:

- `const seats = ["bottom","top","left","right"]`(または並び順違い)という4人固定の配列が
  最低17箇所に直書きされている。用途は表示順・手牌公開・頭ハネ順・点差表示切り替えなど広範囲
  (例: 3323行目, 3362行目, 5237行目, 5388行目, 5409行目, 5555行目, 5627行目 など)
- 牌山生成が136枚固定でハードコード
  (1432行目コメント「136枚の牌山を作る（各種4枚ずつ）」、1446行目「34種類×4枚=136枚」)。
  王牌8枚固定も同様に直書き(1517行目付近)
- 「相手は必ず3人」という前提の割り算・分岐が18箇所以上
  - 役なしツモの精算(5100〜5110行目付近): 「3人合計」を前提に配分を計算
  - 三家和(3人同時ロン)判定(2333〜2341行目): 「3人（自分含む）が全員ロン可能」を
    決め打ちでチェック

つまり「プレイヤー数」と「牌の総数」は、RuleConfigの1プロパティとして外から差し込める形には
なっておらず、コードのあちこちに直接埋め込まれている。3人打ち(108枚・北の特殊処理)を
足すには、この17+18箇所すべてを洗い出して汎用化する作業が必要になる。

## 現状評価: 現行ルール自体、まだ「完成」ではない

基準.mdの「次のアクション」に、現行の無限ルールNEOだけでもまだ残っているタスクがある。

- 未実装の役: 海底摸月・河底撈魚・嶺上開花・槍槓・ダブルリーチ・九蓮宝燈
- 途中流局4種(九種九牌・四風連打・四家立直・四開槓/五開槓)が未実装
- 四槓子(役満)が未実装: `三槓子`判定が「3組以上」の条件になっているため、4組カンが
  揃っても四槓子ではなく三槓子3翻のまま計算されるバグ
- 対局終了時に残った供託が誰にも配分されない既知の穴
- ダブロン精算・リーチ後暗槓の猶予表示・頭ハネ順など、直近の修正がnode --checkのみで
  実機(ブラウザ)確認が済んでいない項目が複数

土台が完全に固まっていない状態で「モードを10種に増やす」という横展開を始めると、
バグの発生源が「新しいモード側の実装ミス」なのか「元々あった未消化のバグ」なのか
切り分けにくくなり、テスト配布中の知人からのフィードバックとも交錯しやすい。

---

## 推奨ロードマップ

### フェーズ0: 現行ルール(無限ルールNEO)の残タスク消化(先行推奨)

上記の未実装役・途中流局4種・四槓子バグ・供託未配分の穴を先に埋める。ここを土台にしないと、
以降どのモードを増やしても同じ穴を10倍複製することになる。

### フェーズ1(低リスク・着手しやすい): 4人打ち・136枚の前提を変えないプリセット差分

無限Original、MリーグOriginal/NEO、WWスタンダード/プレミアムの4〜5モードは、
「プレイヤー数」「牌の総数」に手を入れずに、以下のような**値レベルのRuleConfig**だけで
実現できる可能性が高い。

- 赤ドラ枚数(0/1種ずつ/現行の枚数)
- 一発・裏ドラ・カンドラの有効/無効
- MCR役(独自役満マッピング)の有効/無効
- ウマ・オカのテーブル(現行の「浮き人数で切り替え」方式 vs Mリーグ公式10-30 vs WW方式)
- ノーテン罰符の有無・金額
- 出発点・返し点

これらは既存の関数(settleYakunashi系、順位点計算部など)に「設定オブジェクトを1つ増やして
参照させる」形で対応できるはずで、17箇所の`seats`配列や136枚生成には触れずに済む。
Geminiの提案のうち、この部分(4人打ちをフラグで量産する)は妥当な指摘だと考える。

### フェーズ2(中リスク・独立スコープ推奨): 3人打ち(無限NEO三人版・ザンリーグ)

108枚化・北の抜き寄せ/客風牌としての扱い・3人固定ロジックの汎用化(前述の17+18箇所)が
必要になる。これは「フラグ1つ」では済まず、実質的に核となる進行・精算ロジックの
書き直しに近い。フェーズ1が一段落してから、独立した改修としてスコープを切ることを推奨。
無理に同じRuleConfigに「プレイヤー数」を混ぜ込もうとすると、4人打ち側(フェーズ1で
安定させたばかりの部分)に予期しない影響が及ぶリスクがある。

### フェーズ3(別モジュール推奨): MCR国際公式ルール

MCRは役の数(81種)・点数体系(88点/64点などの生点数、8点縛り)・フリテン無し・リーチ無しなど、
現行の「翻数×500+役満固定額」という評価モデルと根本的に異なる。Gemini自身も
「開発最大の難所」と認めている通り、これは**ルールのバリエーションというより別ゲーム**に近い。
既存の役判定エンジン(candidates.reduceで最大翻を選ぶ方式)に無理に統合しようとすると、
4人打ち側の安定した判定ロジックにまで影響が及びかねない。MCRは「モードの1つ」として
共通RuleConfigに含めるのではなく、独立した判定モジュール(将来的には別プロジェクトとして
切り出す選択肢も含めて)として扱うことを推奨する。

---

## 次にユーザー側で判断してほしい点

1. フェーズ0(現行ルールの残タスク消化)を先にやるか、フェーズ1(低リスクな4人打ち量産)と
   並行して進めるか
2. フェーズ1のRuleConfigに含める項目(上記の値リスト)で過不足がないか
3. 3人打ち・MCRは「いつかやる別プロジェクト」として一旦バックログに置くか、
   それとも今の時点でザックリとでも設計だけ進めておきたいか

方針が決まれば、ローカルのClaude Code CLIへの依頼文の下書きとして以下のような形が使える。

> 「基準.mdのフェーズ0未消化タスク(未実装役・途中流局4種・四槓子バグ・供託未配分)を
> 順に直してください。その後、settleYakunashi・順位点計算・ドラ処理まわりに
> RuleConfigオブジェクト(赤ドラ枚数/一発裏ドラ有無/MCR役有無/ウマオカテーブル/
> ノーテン罰符)を導入し、既存の無限ルールNEOの設定をデフォルト値として動作を変えずに
> 通す形にしてください。3人打ち・MCR関連のプレイヤー数/牌数のハードコード箇所には
> 触らないでください。」

---

## 追記(2026-09-12): ユーザー判断を受けた更新

**判断**: フェーズ0(現行ルールの残タスク)は実戦半荘20回で問題が出ておらずレアケースと
判断、フェーズ0を先行完了条件にはせず、フェーズ1(無限Original)と並行で進める方針に決定。

**無限Originalの実装コストを再調査した結果(上方修正)**:

赤ドラ(あかドラ)まわりの全処理が実際にどこから発生しているかをindex.html全体で
追跡したところ、発生源は`buildWall()`内の**1箇所だけ**であることを確認した。

```js
// index.html 1432〜1446行目付近
if(t.num === 5 && (t.suit === "man" || t.suit === "pin" || t.suit === "sou") && i === 0){
  tile.red = true;
}
```

CPU AIの赤牌温存判断(4996〜5022行目)、得点計算のakaBonus(5082〜5085行目、および
attemptWin系の各所でのakaCount集計)、見た目のハイライト(赤枠・赤い牌画像)は、
すべてこの`tile.red`フラグから導出される作りになっており、独立した「赤あり/なし」の
分岐が別途どこかに埋め込まれているわけではなかった。**つまり、この1箇所を
`if(config.akaDoraEnabled && ...)`のようにガードするだけで、赤ドラを生成しない限り
下流のCPU判断・得点計算・見た目は自動的に「赤なしルール」として整合的に振る舞う。**

前回の提案書では「フェーズ1は値レベルのRuleConfigで対応可能なはず」という見込みで
書いたが、無限Originalに限っては実際にコードを追跡して裏付けが取れた形になる
(3人打ち・MCRのような大改修とは性質が異なることが確認できた)。

**未決定・要ユーザー判断**: モードの切り替え方をどう見せるか
- (a) コード内の設定値を書き換えて別ビルドとして配る(実装コスト最小、配布の手間は増える)
- (b) 対局開始前に簡易選択画面を出す(既存の起家サイクル機能(localStorage、
  `mugenMahjongRedStartWindCycleIndex`)と同じパターンで、選択したルール名を
  localStorageに保存しておける)
- (c) index.htmlを丸ごと複製して別ファイルにする(保守が二重になるため非推奨)

(b)が一番自然だが、UIの手間が増える分だけスコープが広がる。まずは(a)で内部的に
動くことを確認してから(b)のUIを足す、という2段階に分けるのも一案。

---

## 追記2(2026-09-12): 表紙画面の仕様確定

ユーザー要望により、モード切り替えは(b)の「対局開始前の選択画面」に決定。具体的な
表紙仕様は以下の通り。

### 画面内容
- タイトル: 「AI麻雀 聖龍」
- キャッチコピー: 「現役麻雀プロが開発したAI麻雀」「10種類の麻雀ルールに対応」
- ルールボタン10個(表示順は下記):
  1. 赤入り無限ルールNEO ← **実装済み(現行のデフォルト動作)**
  2. 無限ルールOriginal ← **フェーズ1で実装予定(赤ドラ生成1箇所のガードのみ)**
  3. MリーグOriginal ← 未実装
  4. MリーグNEO ← 未実装
  5. WWスタンダード ← 未実装
  6. WWプレミアム ← 未実装
  7. 無限ルールNEO(3人打ち) ← 未実装・大改修
  8. ザンリーグルール ← 未実装・大改修
  9. 複式MCR ← 未実装・大改修
  10. MCR-NEO ← 未実装・大改修

### ボタン挙動
- 10個とも常時クリック可能な状態で表示する(グレーアウト・非表示にはしない)
- 実装済み(1・2)を押す→そのルールの対局室(既存の卓画面)に入る
- 未実装(3〜10)を押す→対局室には入らず、「工事中です、近日公開予定」という
  メッセージを表紙画面上に表示する(ブラウザ標準の`alert()`はモーダルで
  UXが硬いため非推奨。画面内にメッセージ欄を用意し、そこにテキストを出す方式を推奨)

### 実装メモ(ローカルのClaude Code CLIへの依頼で明記すべき点)
- 表紙画面(`#titleScreen`)は、既存の対局画面より手前に表示する新規のトップ画面。
  現状のアプリには表紙・メニューの概念が無い(起動直後にいきなり卓が始まる作り)ため、
  ゼロから追加する新規UI
- ルールごとの設定は`RULESETS`のような配列/オブジェクトで一元管理し、各要素に
  `id, label, status("ready"|"comingSoon"), ruleConfig`を持たせる。`ruleConfig`は
  実装済みの2つ分だけでよい(例: NEO→`{akaDoraEnabled:true}`、Original→
  `{akaDoraEnabled:false}`)
- 選択したルールIDは、既存の半荘状態保存の仕組み(`savedMatchState`
  ＝sessionStorageで局送りのreloadをまたいで保持している値)に含めて保存すること。
  含めないと、次局に進むreloadのタイミングでルールがデフォルト(NEO)に戻ってしまう
- `buildWall()`内の赤ドラ生成箇所(1432〜1446行目付近)を、選択中の`ruleConfig.akaDoraEnabled`
  を見るガード付きに変更する。これが無限Originalの実質的な差分のほぼ全て
- 【決定】既存の「リピート」「次へ」(通常時)/「リピート」「新しい半荘へ」(オーラス後、
  #resultActions)の挙動は一切変更しない。代わりに、この並びに3つ目のボタン
  「ホームページに戻る」を追加し、押すと表紙画面(`#titleScreen`)に戻る。
  通常時・オーラス後のどちらの#resultActionsにも常設する(showNormalResultActions/
  showFinalResultActionsの両方に追加する形)
  - 実装メモ: 半荘の途中(次局へがまだ残っている状態)で「ホームページに戻る」を押した
    場合、進行中の半荘を打ち切って表紙に戻ることになる。誤タップでの巻き戻り事故を
    避けたいなら、この場合だけ簡単な確認(例: window.confirm、または画面内の
    「本当に戻りますか?」の一言)を挟む案があるが、必須ではないのでローカルの
    Claude Code CLI側の判断で決めてよい
  - 「ホームページに戻る」を押した時の内部処理は、既存の「新しい半荘へ」と同じく
    clearMatchState()相当の後処理をしてから、location.reload()ではなく表紙画面を
    表示する分岐にする(表紙で選んだルールがsavedMatchStateに反映される前提のため)

---

## ローカルのClaude Code CLIへの依頼文(下書き・フェーズ1第一歩)

> mugen-mahjong-redのindex.htmlに表紙画面を追加してください。表紙は起動直後に表示し、
> タイトル「AI麻雀 聖龍」、キャッチコピー「現役麻雀プロが開発したAI麻雀」
> 「10種類の麻雀ルールに対応」、そして以下10個のルールボタンを表示します。
>
> 1. 赤入り無限ルールNEO(実装済み・現行動作) 2. 無限ルールOriginal 3. MリーグOriginal
> 4. MリーグNEO 5. WWスタンダード 6. WWプレミアム 7. 無限ルールNEO(3人打ち)
> 8. ザンリーグルール 9. 複式MCR 10. MCR-NEO
>
> 1と2以外(3〜10)はボタンを押すと対局室には入らず、「工事中です、近日公開予定」を
> 表紙画面内に表示してください(alert()は使わず画面内のメッセージ欄で)。
>
> 1(赤入り無限ルールNEO)を押すと現行動作のまま対局室に入ります。2(無限ルールOriginal)は
> buildWall()内の赤ドラ生成(1432〜1446行目付近の`tile.red = true`の分岐)を
> ruleConfig.akaDoraEnabledで判定するように変更し、Originalではfalseにしてください。
> それ以外(CPU AIの温存判断・得点計算・見た目のハイライト)はtile.red/akaCountから
> 自動的に導出される作りなので変更不要のはずです。
>
> 選択したルールIDは既存のsavedMatchState(sessionStorage)に含めて保存し、半荘途中の
> 局送りreload後もルールがデフォルトに戻らないようにしてください。
>
> 既存の結果画面のボタン(#resultActions、通常時の「リピート」「次へ」、オーラス後の
> 「リピート」「新しい半荘へ」)はそのままの挙動を残し、3つ目のボタン
> 「ホームページに戻る」を両方のパターンに追加してください。押すと表紙画面に戻ります。
> 半荘の途中で押すと進行中の半荘を打ち切ることになるので、誤タップ対策として確認を
> 挟むかどうかは実装しやすい方で構いません。
>
> 3人打ち・MCR関連のプレイヤー数(seats配列)・牌数(136枚固定)のハードコード箇所には
> 今回は一切触らないでください。

---

## 追記3(2026-09-13): 無限Originalの実際の差分が判明(想定より大きい)

ユーザーへのヒアリングの結果、無限ルールOriginalと無限ルールNEOの違いは赤ドラだけでは
なく、以下の通り実質13項目に及ぶことが判明した。実コード(index.html)を照合し、
それぞれ「値の差し替えで済むか」「新しい分岐ロジックが要るか」を判定した。

| 項目 | 無限ルールNEO(現行) | 無限ルールOriginal | 対応の性質 |
|---|---|---|---|
| 赤ドラ | あり(各色1枚) | なし | 値(buildWall 1箇所) |
| 開始点/返し点 | 35,000/40,000 | 30,000/30,000 | 値(複数箇所だが単純な差し替え) |
| ウマ・オカ | 浮き人数で3パターン切替+オカ20,000 | 固定: トップ+10,000/2,3着0/ラス−10,000、オカなし | 値(テーブル差し替え、むしろ単純化) |
| 役なし和了 | できる | できない(常に1翻縛り) | **新規ロジック**(勝ち判定にhan≥1のガードを追加) |
| 役の種類 | カスタム含む約35種 | 同じ | 変更不要 |
| 喰いタン・後付け | あり | あり | 変更不要 |
| リーチドラ | あり | あり | 変更不要 |
| 複数ロン | ダブロン(頭ハネにしない)＋邪魔ポン | 頭ハネ(単独勝者)、邪魔ポンなし | **新規ロジック**(findAllRonWinnersの結果から先頭1人だけ採用、邪魔ポンの3秒窓を無効化) |
| 王牌8枚・暗槓表示 | 現行仕様 | 同じ | 変更不要 |
| 親の連荘条件 | 上がり連荘(流局は常に親交代、`proceedOrEndMatch(false, [])`で決め打ち) | 聴牌連荘(流局時、親がテンパイなら連荘) | **新規ロジック**(流局時の連荘判定を親のテンパイ有無で分岐) |
| 本場の扱い | 親交代のたび0にリセット(`computeNextMatchState`) | リセットせず増え続ける | **新規ロジック**(親交代時の本場リセットを条件分岐に) |
| 本場ボーナス | ロン1,500/本場、ツモ各500×3人/本場 | ロン300/本場、ツモ各100×3人/本場(一般的な相場) | 値 |
| ノーテン罰符 | ノーテンの人がテンパイの人それぞれに1,000点(`settleRyuukyoku`) | 場3,000点固定プールを人数で均等割り | **新規ロジック**(計算式そのものが異なる) |

**更新した評価**: 赤ドラの時にお伝えした「1箇所だけ」という見立ては、無限Original全体には
当てはまらなかった。実際には13項目中5項目(役なし和了禁止・複数ロン方式・親の連荘条件・
本場リセット・ノーテン罰符の計算式)が値の差し替えでは済まず、新しい条件分岐が必要になる。

とはいえ、これは「別のエンジンやファイルを新たに作る」必要があるという意味ではない。
牌の判定(役の約35種・手牌分解)・4人打ち136枚のゲーム進行・CPU AI・UI表示は完全に共通の
ままでよく、上記5項目それぞれが「ruleConfigのある値を見て、既存の1〜2箇所の分岐を
追加する」レベルの変更に収まる。ただし合計すると赤ドラ1箇所だけよりは明確に規模が大きく、
このアプリがこれまで小さな変更でも複数回リグレッションを起こしてきた実績を踏まえると、
**13項目を1コミットでまとめて実装するのではなく、段階的に実装・検証することを推奨する**。

推奨する実装順序(それぞれ実機/vmハーネスでの検証を挟む):
1. 値だけで済む項目をまとめて実装(赤ドラ・開始点/返し点・ウマオカ表・本場ボーナス額)
2. 役なし和了の禁止(勝ち判定のガード追加)
3. 複数ロンの単独勝者化＋邪魔ポン無効化
4. 親の連荘条件(聴牌連荘)＋本場の非リセット化(この2つは`computeNextMatchState`/
   `proceedOrEndMatch`まわりで隣接するため同時が効率的)
5. ノーテン罰符の計算式変更

**確認事項**: 供託(リーチ棒)の扱い(その局でリーチして和了した者だけが受け取る、
半荘終了時に残っていたらトップの取り分)はNEOと同じという理解で進めるが、
違いがあれば教えてほしい。

### 追加のヒアリング内容(同日)

さらに以下の項目が判明。表を更新する。

| 項目 | 無限ルールNEO(現行) | 無限ルールOriginal | 対応の性質 |
|---|---|---|---|
| 飛びによる即終了 | あり(`checkMatchEnd`が"bust"を検出し即終了) | なし | 値/条件の削除(checkMatchEndの"bust"分岐をスキップするだけ) |
| 70,000点超えのコールド終了 | あり | なし | 値/条件の削除(同、"over70000"分岐をスキップ) |
| 途中流局(九種九牌・四風連打等) | **NEOでも未実装**(基準.mdの残タスク) | なし(=NEOと同じく実装しない) | **変更不要**(両方とも無いままでよい) |
| W役満(複数役満の重ね取り) | **NEOも現状は単一候補の最大値選択方式で、役満は常に固定額**(重ね取りは元々発生しない) | なし | **変更不要**(現状の実装のままで既に条件を満たしている) |
| 流し役満 | **NEOでも未実装** | なし | **変更不要** |

このヒアリングで、当初懸念した項目のうち3つ(途中流局・W役満・流し役満)は
「NEOに元々無い機能をOriginalでも作らない」という話に過ぎず、実装コストはゼロと判明した。
飛び・コールド終了の2つも、既存の`checkMatchEnd`内の分岐を無効化するだけなので低リスク。
新規ロジックが必要な項目は変わらず5つ(役なし和了禁止・複数ロン単独勝者化・親の連荘条件・
本場非リセット・ノーテン罰符計算式)のまま。

**新たな確認事項**: 「飛び終了なし」ということは、対局が終わらないまま半荘の最後まで
マイナス点を持ち越せる、という理解でよいか。その場合、現行NEOにある「飛ばした人が
和了者に10,000点を払う」という精算ルール自体は、Originalではどうなるか(①同じく
0点を割った瞬間に10,000点だけ精算して対局は続行する、②精算自体も無く単にマイナス点の
まま計算を続ける、のどちらか)を教えてほしい。

### 追加のヒアリング内容(2026-09-13 その2)

さらに2項目、以下が判明。いずれもコードを確認したところ、既存の変数・計算結果を
そのまま流用できるため、想定よりずっと簡単に実装できることが分かった。

| 項目 | 無限ルールNEO(現行) | 無限ルールOriginal | 対応の性質 |
|---|---|---|---|
| 一発 | ロン・ツモどちらでも付く | 出和了り(ロン)のみ、自摸では付かない | **新規ロジックだが低リスク**: 一発判定箇所(4017行目・4570行目の2箇所、`if(riichiDeclared[seat] && ippatsuActive[seat])`)に`isTsumo`条件を追加するだけ。`isTsumo`は同じスコープに既に存在する |
| 七対子の4枚使い | 可(同じ牌4枚を2対子として七対子成立、緑一色四枚使い等の役満もこれを利用) | 不可(7種類がそれぞれちょうど2枚ずつでなければ七対子として認めない) | **新規ロジックだが低リスク**: 「7種類が全部ちょうど2枚」という条件は、実は連七対の判定(4540行目`keys.length===7 && keys.every(k=>counts[k]===2)`)として既に計算済み。Originalではこの条件をそのまま七対子成立の必須条件に追加すればよい。4枚使い前提の役満(緑一色四枚使い等)はOriginalでは自動的に不成立になる(標準の七対子2翻、または不成立で他の分解を試みる) |

これで新規ロジックが必要な項目は合計7つ(役なし和了禁止・複数ロン単独勝者化・親の連荘条件・
本場非リセット・ノーテン罰符計算式・一発ツモ無効化・七対子4枚使い禁止)になったが、
後者2つは既存の変数・計算結果の流用で済むため、実装コストへの影響は小さい。

### 飛び終了・飛び賞の最終回答(2026-09-13)

「飛び終了なし・マイナス点はそのまま最後まで持ち越す・飛び賞(10,000点)も無し」と確定。
これはむしろNEOより単純になる変更で、Originalでは以下を丸ごとスキップすればよい。

- `checkMatchEnd`の"bust"分岐(0点未満で即終了) → スキップ(実装済みの通り)
- 飛び精算(飛ばした人が和了者に10,000点払う処理一式) → 丸ごと未使用
- 支払いを0点で止める「0点止め」処理(現行NEOは支払いでマイナスにならないようキャップしている) → Originalは外し、通常の点数移動をそのままマイナスまで許可する

NEO側の「0点止め」や「飛び精算」のコード自体には触れず、Original実行時だけこれらの
分岐を通らないようにする(ruleConfig.tobiEnabled=falseのような1フラグで、該当処理群を
まとめて迂回させる形が良い)。

---

## 最終確定仕様(まとめ): 無限ルールNEO vs 無限ルールOriginal

これまでのヒアリングで出た差分の全項目。以降の依頼文はこの表を正としてローカルの
Claude Code CLIに渡す想定(これより前の版の依頼文は、この表に統合されたため参考情報)。

| # | 項目 | NEO(現行) | Original | 対応 |
|---|---|---|---|---|
| 1 | 赤ドラ | あり(各色1枚) | なし | 値 |
| 2 | 開始点/返し点 | 35,000/40,000 | 30,000/30,000 | 値 |
| 3 | ウマ・オカ | 浮き人数別3パターン+オカ20,000 | 固定(トップ+10,000/2,3着0/ラス−10,000)、オカなし | 値 |
| 4 | 本場ボーナス | ロン1,500/本場、ツモ各500×3人/本場 | ロン300/本場、ツモ各100×3人/本場 | 値 |
| 5 | 役なし和了 | 可 | 不可(常に1翻縛り) | 新規ロジック |
| 6 | 複数ロン | ダブロン(頭ハネにしない)+邪魔ポン | 頭ハネの単独勝者、邪魔ポンなし | 新規ロジック |
| 7 | 親の連荘条件 | 上がり連荘(流局は常に親交代) | 聴牌連荘(流局時、親がテンパイなら連荘) | 新規ロジック |
| 8 | 本場のリセット | 親交代のたび0にリセット | リセットせず増え続ける | 新規ロジック |
| 9 | ノーテン罰符 | ノーテン1人がテンパイ1人に1,000点(人数分乗算) | 場3,000点固定プールを均等割り | 新規ロジック |
| 10 | 一発 | ロン・ツモどちらも付く | ロン(出和了り)のみ | 新規ロジック(低リスク、isTsumoの流用) |
| 11 | 七対子4枚使い | 可 | 不可(7種類ちょうど2枚ずつのみ) | 新規ロジック(低リスク、既存の連七対判定条件の流用) |
| 12 | 飛びによる即終了 | あり | なし | 条件を無効化するだけ |
| 13 | 70,000点コールド終了 | あり | なし | 条件を無効化するだけ |
| 14 | 飛び賞(10,000点)・0点止め | あり | なし(マイナス点をそのまま持ち越す) | 該当処理一式を丸ごとスキップ |
| 15 | 途中流局・W役満・流し役満 | 未実装(NEOにも無い) | 同じく無し | 変更不要 |
| 16 | 喰いタン後付け・リーチドラ・王牌8枚固定・暗槓中国式伏せ | あり | 同じ | 変更不要 |
| 17 | 役の種類(約35種) | 実装済み | 同じ | 変更不要 |
| 18 | 供託(リーチ棒)の扱い | 和了者が受取、残りはトップへ | 同じという前提で進行(要最終確認) | 変更不要(前提) |

## ローカルのClaude Code CLIへの依頼文(最終版・これ以前の依頼文を差し替え)

> mugen-mahjong-redのindex.htmlに、既存の「赤入り無限ルールNEO」に加えて
> 「無限ルールOriginal」を実装してください。両ルールはこのファイル内の
> 「最終確定仕様(まとめ)」の表(項目1〜18)の通りに差分があります。表紙画面
> (`#titleScreen`)は実装済みなので、ボタン2「無限ルールOriginal」を選んだ時だけ
> Original側のruleConfigが使われるようにしてください。
>
> 実装は1コミットにまとめず、次の順序で段階ごとにコミット・検証してください。
> 1. 値の差し替えのみの項目(1〜4、12〜14): ruleConfigに`akaDoraEnabled`
>    `startingPoints``returnPoints``umaTable``okaEnabled``honbaRonBonus`
>    `honbaTsumoBonusPerSeat``earlyEndOnBust``earlyEndOnOver70000``tobiEnabled`
>    のようなフィールドを追加し、Original用の値を設定。NEO用のデフォルト値は
>    現行の動作と完全に一致させること(既存のNEOプレイに影響が出ないことを最優先)
> 2. 役なし和了の禁止(項目5): 和了判定でhan===0(役なし)の場合、Originalでは
>    和了を成立させない
> 3. 複数ロンの単独勝者化+邪魔ポン無効化(項目6): `findAllRonWinners`の結果から
>    Originalでは先頭1人だけを採用し、邪魔ポンの3秒割り込み窓を発生させない
> 4. 親の連荘条件+本場リセット(項目7・8): 流局時の連荘判定を、Originalでは
>    親がテンパイかどうかで決める。`computeNextMatchState`のrenchan=false分岐で、
>    Originalでは本場をリセットしない
> 5. ノーテン罰符の計算式変更(項目9): `settleRyuukyoku`をOriginal用に、場3,000点を
>    テンパイ人数で均等に受け取り、ノーテン人数で均等に支払う方式に変更
> 6. 一発ツモ無効化(項目10)・七対子4枚使い禁止(項目11): それぞれ既存の`isTsumo`・
>    連七対判定の条件を流用して追加
>
> 各段階の後、既存のNEOルールで一切のリグレッションが無いこと(node --checkと
> 可能な範囲のvmハーネス検証)を確認してから次の段階に進んでください。
>
> 3人打ち・MCR関連のプレイヤー数(seats配列)・牌数(136枚固定)のハードコード箇所には
> 今回は一切触らないでください。

